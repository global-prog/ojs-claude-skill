# OJS Theme Development (3.4.x / 3.5.x)

Verified against the PKP Theming Guide, `ThemePlugin.php` (identical API 3.4/3.5), the OJS 3.5 default theme, and official theme repos (defaultManuscript, immersion, bootstrap3, healthSciences, classic, pragma). Smarty tags, template variables, menus/sidebar machinery → `frontend-templating.md`.

## 1. Theme plugin skeleton

```
plugins/themes/myTheme/
├── MyThemePlugin.php            # namespaced .php (3.4+)
├── version.xml                  # <type>plugins.themes</type>, <lazy-load>1</lazy-load>
├── styles/index.less            # entry point (+ variables.less, rtl.less…)
├── js/main.js
├── locale/en/locale.po
└── templates/…                  # overrides, mirroring core paths
```

```php
<?php
namespace APP\plugins\themes\myTheme;

use PKP\plugins\ThemePlugin;

class MyThemePlugin extends ThemePlugin
{
    public function init()
    {
        // ALL configuration here; runs only for the active theme (+ parents)
        $this->addStyle('stylesheet', 'styles/index.less');
        $this->addScript('main', 'js/main.js');
        $this->addMenuArea(['primary', 'user']);
    }

    public function getDisplayName() { return __('plugins.themes.myTheme.name'); }
    public function getDescription() { return __('plugins.themes.myTheme.description'); }
}
```

⚠️ The standalone official theme repos (classic, immersion, healthSciences, pragma) still ship legacy `.inc.php` + `HookRegistry::add` even on their `stable-3_5_0` branches — do **not** copy that style for new themes; only their *patterns* (below). `pkp/academicFree` does not exist.

## 2. ThemePlugin API (verified, identical 3.4/3.5)

```php
abstract public function init();
public function addStyle($name, $style, $args = [])     // args: context ('frontend'|'backend'|custom),
                                                        //       priority, addLess[], addLessVariables, inline
public function addScript($name, $script, $args = [])  // args: context, priority, inline
public function addOption($name, $type, $args = [])    // type: any Field* except FieldUpload/FieldUploadImage
public function addMenuArea($menuAreas)                 // string|array
public function modifyStyle($name, $args = [])         // merge args into an existing (incl. parent) style
public function removeStyle($name) / removeScript($name) / modifyScript($name, $args = [])
public function setParent($parent)                      // parent plugin name, lowercased
public function getOption($name) / getOptionsConfig() / getOptionValues(?int $contextId)
public function removeOption($name) / modifyOptionsConfig()
```

`.less` files auto-compile server-side (cached — clear `cache/` after changes). `addLess` (array of extra LESS files) and `addLessVariables` (string of `@var: value;` overrides) are the customization mechanism. `$min = Config::getVar('general', 'enable_minified') ? '.min' : ''` is the standard toggle for shipping minified assets.

## 3. Default theme LESS variables (verified `styles/variables.less`, OJS 3.5)

| Variable | Default | Notes |
|---|---|---|
| `@bg-base` | `#1E6292` | **header/accent background — this is what the "Base Colour" theme option writes** |
| `@primary` | `#006798` | links/buttons — a SEPARATE variable (common trap!) |
| `@primary-lift` | `lighten(@primary, 10%)` | hover states |
| `@bg` / `@bg-shade` | `#fff` / `#ddd` | page background / shaded areas |
| `@text` / `@text-light` | `rgba(0,0,0,0.87)` / `rgba(0,0,0,0.54)` | |
| `@text-bg-base` | `#fff` | text on `@bg-base` (default theme auto-darkens it for light base colours via `isColourDark()`) |
| `@font` | Noto Sans stack | `@font-heading`, `@font-site-title` default to it |
| `@rem` | `14px` | root size; spacing scale `@base 0.714rem`, `@double 1.43rem`, `@triple`, `@quadruple` |
| `@screen-phone/tablet/desktop/lg-desktop` | 480/768/992/1200px | each has a `-container` variant (−40px) |
| `@sidebar-width` | `300px` · `@radius` `3px` · `@normal/@bold` `400/700` | |

Default theme `styles/`: `index.less` (entry; imports pkp-lib shared `fontawesome/normalize/variables/utils/helpers/orcid.less` first, then theme files, **`rtl.less` last**), `variables.less`, `body/head/main/components/footer/sidebar.less`, `components/`, `fonts/`, `objects/`, `pages/`.

## 4. Child themes — canonical pattern (verified: pkp/defaultManuscript)

```php
public function init()
{
    $this->setParent('defaultthemeplugin');

    // Append child LESS into the parent's already-registered stylesheet:
    $this->modifyStyle('stylesheet', ['addLess' => ['styles/index.less']]);

    // Curate inherited options:
    $this->removeOption('typography');
    $this->removeOption('useHomepageImageAsHeader');
    $this->addOption('accentColour', 'FieldColor', [
        'label' => __('plugins.themes.defaultManuscript.option.accentColour.label'),
        'default' => '#F7BC4A',
    ]);

    // Inject computed variable overrides:
    $additionalLessVariables = [];
    if ($this->getOption('baseColour') !== '#1E6292') {
        $additionalLessVariables[] = '@bg-base:' . $this->getOption('baseColour') . ';';
    }
    $this->modifyStyle('stylesheet', ['addLessVariables' => join('', $additionalLessVariables)]);
}
```

- Parent's `init()` runs first; child then modifies/removes parent assets and options. Multi-level inheritance works.
- Override variables in child LESS too: `@primary: #4b7d92; @font-heading: "Montserrat", sans-serif;`.
- defaultManuscript ships **no templates/ dir** (pure restyle) and **its own `rtl.less`** — both legitimate choices.

## 5. Template hierarchy & overrides

Lookup order (first wins): **child theme → parent theme → app `templates/` → `lib/pkp/templates/`**. Override by mirroring the exact path, e.g. `plugins/themes/myTheme/templates/frontend/objects/article_details.tpl`. Full inventory of what exists to override → `frontend-templating.md` §3. **Clear `cache/t_compile/` after template edits.**

Immersion (most heavily templated official theme) overrides ~27 page templates + all 6 OJS objects — read it for full-redesign patterns.

## 6. Patterns from official themes (all verified in source)

- **Custom data for templates** (healthSciences, immersion): hook `TemplateManager::display`, guard on template name, `$templateMgr->assign(...)`. Only top-level `frontend/pages/*` templates fire it usefully.
- **Themes are full plugins — they may register hooks and extend entities** (immersion): `Schema::get::issue` to add a per-issue colour property + `issueform::display/initdata/readuservars/execute` form hooks + `Templates::Editor::Issues::IssueData::AdditionalMetadata` backend injection. Use sparingly; it couples the theme to data.
- **Build pipelines** (immersion, healthSciences): Gulp 4 + Dart Sass + Bootstrap 5 → committed `resources/dist/app.min.css|js`, loaded via plain `addStyle`/`addScript`. LESS-free themes are fine — `addStyle` with `.css` works.
- **Style variants as an option** (bootstrap3): a `FieldOptions` radio chooses among 18 pre-compiled Bootswatch LESS entries; `init()` conditionally `addStyle`s the chosen one.
- **Scoped asset contexts** (immersion): `addStyle('htmlGalley', '...', ['contexts' => 'htmlGalley'])` styles the HTML-galley viewer; `['contexts' => 'backend-manageIssues']` loads an asset only on a specific backend page.
- **Homepage image as header** (default theme): `addStyle('homepageImage', '.pkp_structure_head { background: ... }', ['inline' => true])`.

## 7. RTL support (Arabic, Hebrew, Farsi…)

OJS sets `dir="rtl"` on `<body>` automatically for RTL locales (`$currentLocaleLangDir`). Three verified mechanisms:

1. **Default theme:** ship `styles/rtl.less`, imported **last**, keyed on the attribute selector:
   ```less
   body[dir="rtl"] { direction: rtl; unicode-bidi: embed; }
   body[dir="rtl"] .pkp_site_name { text-align: right; }  /* + float/margin/position flips per breakpoint */
   ```
2. **bootstrap3:** runtime server-side detection to load a separate compiled RTL bundle:
   ```php
   if (Locale::getMetadata(Locale::getLocale())->isRightToLeft()) {
       $this->addStyle('bootstrap-rtl', 'styles/bootstrap-rtl.min.css');
   }
   ```
3. **Child themes** (defaultManuscript, healthSciences): ship their own `rtl.less` rather than relying on the parent's.

## 8. Theming the editorial backend (3.5)

- Officially supported but discouraged beyond brand colours: `addStyle('backend', 'styles/backend.css', ['context' => 'backend'])` and `modifyStyle('pkpLib', ['addLessVariables' => ...])`.
- The 3.5 editorial UI is Vue 3 + Tailwind; no official theme rewrites Vue components — backend theming = CSS/template injection around the Vue app, nothing more.
- Reader-facing frontend remains Smarty + LESS in 3.5; the ThemePlugin API did not change from 3.4.

## 9. Theme debugging checklist

1. Theme not listed → `version.xml` type/lazy-load/class-dir naming mismatch; check PHP error log for namespace errors.
2. Styles not updating → compiled LESS is cached: clear `cache/`, hard-reload.
3. Template edit invisible → wrong mirror path or stale `cache/t_compile/`.
4. Colour option does nothing → you overrode `@primary` but the option writes `@bg-base` (§3), or no `addLessVariables` re-injection in `init()`.
5. Option not applying → options are per-context, saved in Website Settings → Appearance; `getOption()` returns defaults only before first save.
6. RTL broken → ensure `rtl.less` imports last; verify `body[dir="rtl"]` actually set (locale metadata).

## 10. Building a complete custom theme — production lessons (all source-verified, 3.5)

Hard-won gotchas from building a full bespoke theme (standalone CSS, custom header/footer/homepage, dynamic data). Each caused a real bug.

**A re-uploaded plugin update silently does NOTHING unless you bump `<release>` in `version.xml`.** This is the #1 cause of "my fix didn't take effect / settings still look wrong" after re-installing. OJS compares the uploaded plugin's version with the installed one; if it is the **same or lower**, the Plugin Gallery upload is rejected/ignored and the **old files stay in place** — so none of your template/CSS/option changes appear and the admin keeps seeing the previous (e.g. empty/broken) build. Increment the four-part version for **every** rebuild you want installed (`1.0.1.0` → `1.0.2.0` → …), re-upload, then **Clear Data Caches**. When iterating, treat "bump version.xml" as part of every package step, not an afterthought.

**Multilingual theme-option fields read as empty in the settings form** even though the rest works — see the dedicated multilingual section: a multilingual field needs a **locale-keyed default object** (a scalar leaves it blank), and to never show a blank tab, fill **every** supported locale (`$ctx->getSupportedLocales()`), not just `en`.

**Locale: `##plugins.themes.x.key##` showing on the live site.**
- Symptom: every theme string renders as `##key##` while core English works.
- Cause: OJS does **not** rebuild the compiled locale cache when a plugin is installed. The keys load fine, the cache is just stale.
- Fix: **Administration → Clear Data Caches** (deletes `cache/fc-*`). This is the #1 post-install support answer.
- Folder MUST be the short code: `locale/en/locale.po`, `locale/ar/locale.po` (not `en_US`). `Plugin::addLocaleData()` registers the `locale/` dir via `Locale::registerPath()`; it runs from `LazyLoadPlugin::register()` (line 37), which `ThemePlugin` inherits — so no manual call needed.
- `.po` must be valid gettext: balanced `msgid`/`msgstr`, and **escape inner quotes** (`\"Spectral\"`) or loading silently fails. Validate with `msgfmt -c` (NOT a line counter).
- **CRITICAL — put a blank line between every entry.** OJS 3.5's PO loader separates entries on blank lines; consecutive `msgid`/`msgstr` pairs with *no* blank line between them get silently dropped, keeping only the **last** entry of each blank-line-delimited block. Symptom: a *positional* `##key##` pattern where only the last option in each section resolves and the rest show `##` (looks like a partial cache miss but isn't). Match the default theme: one blank line after every entry. Real fix that caused this: a hand-written `.po` with no inter-entry blank lines.

**Removing the default stylesheet drops base utility CSS.** A child theme that does `removeStyle('stylesheet')` (to ship its own CSS) loses utilities the default sheet provided — most visibly **`.cmp_skip_to_content`** (the skip links become *visible text* at the top of every page) and `.pkp_screen_reader` / `.pkp_helpers_display_none`. Re-add them:
```css
.cmp_skip_to_content a{position:absolute;inset-inline-start:-2000px;width:1px;height:1px;overflow:hidden;}
.cmp_skip_to_content a:focus{inset-inline-start:0;width:auto;height:auto;overflow:visible;}
.pkp_screen_reader{position:absolute!important;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap;}
.pkp_helpers_display_none{display:none!important;}
```

**Header/footer wrapper contract** (must match exactly or pages break). Header opens: `<body>` → `pkp_structure_page` → `pkp_structure_head`(header) → nav → `pkp_structure_content` → `pkp_structure_main`. Footer closes: `pkp_structure_main` → (sidebar) → `pkp_structure_content` → footer wrapper → `pkp_structure_page` → `</body>`. Include `headerHead.tpl` (it emits the whole `<head>`; don't add your own) and `skipLinks.tpl`. Capture `homeUrl` yourself (`{url page="index" router=PKP\core\PKPApplication::ROUTE_PAGE}`) — it is **not** a global.

**Sidebar layout via the `.has_sidebar` modifier**, not `:has()`. Core adds `has_sidebar` to `pkp_structure_content` when a sidebar exists; key your grid off it. To suppress the footer's block-sidebar on a page with its own bespoke sidebar, `{assign var="isFullWidth" value=true}` **before** the header include (footer checks `{if empty($isFullWidth)}`). Render admin block plugins yourself with `{capture}{call_hook name="Templates::Common::Sidebar"}{/capture}`.

**Dynamic navigation + footer from OJS menus.** Primary nav: `{load_menu name="primary" id="navigationPrimary" ulClass="..."}` (outputs `<ul class="pkp_nav_list"><li><a>`). Register extra areas in `init()` with `$this->addMenuArea(['footerCol1','footerCol2'])`; they become assignable in Settings → Website → Navigation. `{load_menu name=$area}` returns `''` when no menu is assigned → fall back to option links.

**Dynamic homepage stats** via Repo collectors (run only on the homepage template; guard null context — the issue collector throws without a context id):
```php
$articles = Repo::submission()->getCollector()->filterByContextIds([$id])
    ->filterByStatus([PKPSubmission::STATUS_PUBLISHED])->getCount();
$issues = Repo::issue()->getCollector()->filterByContextIds([$id])->filterByPublished(true)->getMany();
```
Gotcha: the **issue collector `orderBy` takes no direction** (ORDERBY_DATE_PUBLISHED is hardcoded DESC) — compute the earliest year with `min()` in PHP, not `limit(1)`.

**Rich-text + hyperlinks in theme options.** `FieldRichTextarea` works as a theme option (verified: no allow-list in `ThemePlugin::addOption`; core `PKPMastheadForm` uses it without `uploadUrl`). Defaults give a `link` button. Output with **`|strip_unsafe_html`** (HTMLPurifier; allows `<a href>`,`<p>`,`<strong>`,`<ul>`, strips `<script>`) — never `|escape` (shows literal HTML) or raw (XSS). Do **not** add the `image` plugin without a valid `uploadUrl`.

**Multilingual theme options** (verified working; no core theme uses it but the chain is fully wired): set `isMultilingual` on the field — either `addOption('x','FieldRichTextarea',['isMultilingual'=>true])` or, post-hoc for many, `$this->options['x']->isMultilingual = true;` in a loop after the `addOption` calls. `PKPThemeForm` renders per-locale tabs, the PUT `/contexts/{id}/theme` save (`ThemePlugin::saveOption`) stores the locale-keyed array as JSON (`setting_type=object`), and `getOptionValues` reconstructs it. Retrieve with **`$this->getLocalizedOption('x')`** (NOT `getOption`, which returns the raw `['en'=>…,'ar'=>…]` array). Make the getter robust: fall back to the raw scalar when the localized value is empty, so single-value defaults and pre-multilingual saved values still render:
```php
// NEVER call getLocalizedOption() on a scalar — it fatals (see CRITICAL note below).
$ml = function($k){ $raw=$this->getOption($k); if(is_array($raw)){ try{$v=$this->getBestLocalizedData($raw);}catch(\Throwable $e){$v=null;} return (string)($v ?? ''); } return is_string($raw)?$raw:''; };
```
Keep colours/fonts/toggles/URLs/numbers single-value; make only user-facing TEXT multilingual.

**CRITICAL multilingual-default gotcha (causes "all fields blank" in the settings form).** A multilingual field's default **must be a locale-keyed array**, not a scalar — otherwise the settings form renders an **empty** input. Why: `PKPThemeForm` sets each field's `value` from `getOptionValues()` (saved value, or `null` if unsaved), and `Field::getConfig()` does `value ?? default`. So for an unsaved option the form falls back to `->default`. A multilingual Vue field binds to an object like `{"en":"…"}`; given a scalar string it can't map it to any locale tab and shows nothing. (Non-multilingual fields with scalar defaults pre-fill fine — which is why only the multilingual ones look blank.) Fix: when you flip `isMultilingual`, wrap the default too. The render path (`getOption`/`getLocalizedOption`) works with a scalar, so this is purely for the form UI:
```php
$ctx = Application::get()->getRequest()?->getContext();
$supported = $ctx ? $ctx->getSupportedLocales() : ['en'];   // fill EVERY tab
foreach ($multilingualKeys as $k) {
    $opt = $this->options[$k]; $opt->isMultilingual = true;
    if (is_string($opt->default)) {                 // include EMPTY defaults (see CRITICAL note)
        $base = $opt->default; $obj = [];
        foreach ($supported as $loc) { $obj[$loc] = ($loc === 'ar' && isset($ar[$k])) ? $ar[$k] : $base; }
        $obj['en'] = $obj['en'] ?? $base;
        $opt->default = $obj;
    }
}
```
**Seed every supported locale, not just `en`** — a multilingual field shows blank in any locale missing from the default object, so an `en`-only default looks empty when the admin opens (or the journal's primary is) the Arabic tab. Use the journal's real locales (`$ctx->getSupportedLocales()`), putting translated strings where you have them and the base value elsewhere. **CRITICAL — never leave a multilingual option's default as a bare string, EMPTY ONES INCLUDED** (a tempting "leave blank to use journal data" pattern). A multilingual option whose value is a scalar makes **`getLocalizedOption()` FATAL**: it calls `getBestLocalizedData()`, which type-hints `array $data` and throws `Argument #1 must be of type array, string given`. That uncaught `TypeError` in the `TemplateManager::display` hook **white-screens the theme-settings page AND every frontend page** (your `$ml` getter runs on each). It also commonly only surfaces *after* an upload adds a new multilingual option (its scalar `''` default is now read before anyone re-saves settings). Two defenses, apply BOTH: (1) in the seeding loop wrap **every** string default into a locale-keyed object — drop any `&& $opt->default !== ''` guard; (2) make the `$ml` getter branch on `is_array($raw)` and only then call `getBestLocalizedData($raw)` (wrapped in try/catch), returning a scalar untouched — so a string can never reach the localized resolver. (Separately: don't fall the **masthead subtitle** back to the journal's `searchDescription` — that's usually the journal name again and renders the title twice; make the subtitle an explicit opt-in option.)

**Locale switcher also on the site index (`.../index/{locale}`).** Build the toggle for the main OJS platform/site index too, where there is **no** journal context: source the locales from the site, not just the context — `$locales = $context ? $context->getSupportedLocaleNames() : $request->getSite()->getSupportedLocaleNames();` (both expose `getSupportedLocaleNames()`), and keep only the `PKPPageRouter` guard. `$router->url($request, null, $page, $op, $path, urlLocaleForPage: $code)` then produces correct site-level URLs.

**Locale switcher link — 3.5 embeds the locale in the URL; `user/setLocale` is GONE.** OJS 3.5 removed the `user` page handler entirely (`pages/user/UserHandler.php` no longer exists), so the old `{url page="user" op="setLocale" path=$loc source=…}` link **404s / silently fails** — a real, easy-to-miss bug when porting a 3.3/3.4 theme. The 3.5 mechanism is URL-embedded locale: the `{url}` Smarty function (and `PKPPageRouter::url`) take a **`urlLocaleForPage`** parameter. Because the requested *path args* are not exposed as a template variable (`$requestedPage`/`$requestedOp` are, `requestedArgs` is **not**), build the switch links in PHP from the `TemplateManager::display` hook, mirroring PKPTemplateManager's own hreflang logic, and assign them for the template to iterate:
```php
$request = Application::get()->getRequest();
$router  = $request->getRouter();
$context = $request->getContext();
if ($context && $router instanceof \PKP\core\PKPPageRouter
    && count($locales = $context->getSupportedLocaleNames()) > 1) {
    $page = $router->getRequestedPage($request);
    $op   = $router->getRequestedOp($request);
    $path = $router->getRequestedArgs($request);          // array — the missing template var
    $cur  = \PKP\facades\Locale::getLocale();
    $out  = [];
    foreach ($locales as $code => $name) {
        $out[] = ['code'=>$code, 'name'=>$name, 'current'=>$code===$cur,
                  'url'=>$router->url($request, null, $page, $op, $path, urlLocaleForPage: $code)];
    }
    $templateMgr->assign('myLocaleToggle', $out);          // {foreach}…<a href="{$loc.url|escape}">
}
```
`$supportedLocales` IS a frontend template var (a `code=>name` map from `getSupportedLocaleNames(LANGUAGE_LOCALE_ONLY)`), but it only gives names — it can't build a same-page switch URL on its own. There is no `languageToggle.tpl` include.

**Overriding frontend object/page templates — parity checklist (each omission is a real regression).** When you replace `article_summary`/`issue_toc`/`article_details`/`indexJournal` with bespoke markup, you inherit the handler's assigned vars but must keep the *logic*, or live data silently disappears:
- **`galley_link.tpl` marks every galley `restricted` unless `hasAccess` is truthy in scope.** In `article_details` the handler assigns a global `$hasAccess`, so an include works. In a **TOC/list** (`article_summary`), there is no per-article `$hasAccess` — you must compute it and pass it, or open-access downloads get a `.restricted` class + a "subscription access" screen-reader label: `{assign var=hasArticleAccess value=$hasAccess}{if $currentContext->getData('publishingMode')==APP\journal\Journal::PUBLISHING_MODE_OPEN || $publication->getData('accessStatus')==APP\submission\Submission::ARTICLE_ACCESS_OPEN}{assign var=hasArticleAccess value=1}{/if}` then `{include … hasAccess=$hasArticleAccess purchaseFee=$currentJournal->getData('purchaseArticleFee') purchaseCurrency=$currentJournal->getData('currency')}`.
- **Respect per-article author hiding:** `{if (!$section.hideAuthor && $publication->getData('hideAuthor')==APP\submission\Submission::AUTHOR_TOC_DEFAULT) || $publication->getData('hideAuthor')==APP\submission\Submission::AUTHOR_TOC_SHOW}` — not just `$section.hideAuthor`.
- **Filter TOC galleys to primary files** with `$primaryGenreIds` (skip dependent/supplementary): `{if $primaryGenreIds}{assign var=file value=$galley->getFile()}{if !$galley->getData('urlRemote') && !($file && in_array($file->getGenreId(),$primaryGenreIds))}{continue}{/if}{/if}`.
- **`indexJournal` must call `{call_hook name="Templates::Index::journal"}`** (homepage plugins/carousels hook here) **and render `{$additionalHomeContent}`** (the admin's Settings → Website → Appearance → *Additional Content*) — both are trivially dropped when you rewrite the homepage, silently discarding admin content and breaking plugins.
- **Highlights / spotlights (3.4+) are also dropped** if you rewrite `indexJournal`/`indexSite` — re-add `{if $highlights && $highlights->count()}{include file="frontend/components/highlights.tpl" highlights=$highlights}{/if}`. The core `highlights.tpl` is a **Swiper** carousel (needs Swiper JS+CSS the default theme bundles). A custom theme that doesn't ship Swiper should **override `highlights.tpl`** with a static layout (a card grid) so it works with zero JS — otherwise the slides stack unstyled and the prev/next buttons are dead. Highlight accessors: `getImage()`/`getImageUrl()`/`getImageAltText()`, `getLocalizedTitle()`, `getLocalizedDescription()`, `getUrl()`, `getLocalizedUrlText()`.
- **Site index (`indexSite`, multi-journal) realities:** the site `$about` is full rich CMS HTML with its own H2/H3 sections — render it as **`.prose`** at a readable measure, not oversized lead text. Each journal's `getLocalizedDescription()` is the **journal's entire description** (can be many paragraphs), so a journal-card list becomes a wall of text — **clamp** it (`max-height` + a fade-out `::after` gradient to `var(--card)`, or `line-clamp`) and let "View Journal" lead to the full text. There is no journal context here, so the masthead falls back to `$siteTitle` and the locale toggle must use site locales (see the locale-switcher note).
- **Handler-assigned vars worth preserving:** `$categories`, `$ccLicenseBadge` + `$currentContext->getLocalizedData('licenseTerms')` (copyright block), `$pubIdPlugins` (URN etc.), `$issueGalleys` (full-issue PDF on the issue page), `$userGroupsById` (author role labels), `array_reverse($article->getPublishedPublications())` (version history). `$dateFormatShort`/`$dateFormatLong` are PHP `date()` formats (use with `|date_format` or `DateTime::format`). To keep one `issue_toc.tpl` for both the homepage and the issue page, gate the issue-only chrome (cover, identifiers, full-issue galleys, publish date) behind a `hideCover` flag you pass from the homepage include.
- **Academic author/affiliation formatting:** for numbered-superscript authors with a numbered affiliation list, build the map in PHP (iterate `$author->getAffiliations()`, dedupe by `getLocalizedName()` in order of appearance, assign 1-based numbers, expose each author's `numStr` and each affiliation's `getRor()` for a ROR link), then render — far cleaner than trying to dedupe in Smarty. `getAffiliations()` is an entity array; `getLocalizedAffiliationNamesAsString(null, ', ')` is the quick inline alternative.
- **Inline PDF viewing is NOT a theme concern.** There is no core `articleGalley.tpl` to override — PDF galleys are embedded by the bundled **`pdfJsViewer`** generic plugin (it hooks the galley view and ships its own viewer template). Don't reinvent a PDF embed in the theme; just enable/style around that plugin. (HTML galleys are styled via your `['contexts'=>'htmlGalley']` stylesheet.)
- **Article-page elements easy to miss:** the **article's own cover image** (`$publication->getLocalizedData('coverImage')` → array with `.altText`; URL via `$publication->getLocalizedCoverImageUrl($article->getData('contextId'))`); **non-DOI pub-IDs** (loop `$pubIdPlugins`, skip `getPubIdType()=='doi'`, `$publication->getStoredPubId($type)`, link via `$pubIdPlugin->getResolvingURL($currentJournal->getId(), $pubId)`); and **ORCID** — core assigns `$orcidIcon` / `$orcidUnauthenticatedIcon` (ready-made markup) and `$author->hasVerifiedOrcid()`, so render the real ORCID logo (verified vs not) rather than a text "iD". For brand **share icons** (X/LinkedIn/Facebook) use inline SVG brand paths, not letters/emoji.
- **`article_summary.tpl` is used from THREE places** — issue TOC, homepage current-issue TOC, and the search results page (`search.tpl` includes it as `article=$result.publishedSubmission journal=$result.journal showDatePublished=true hideGalleys=true`). A bespoke override must therefore: honour **`$hideGalleys`** (search hides them); honour **`$journal`** for cross-journal links — `{url journal=$journal->getPath() page="article" …}` and pass `journalOverride=$journal` to `galley_link.tpl`; and handle the **no-`$currentContext`** case (site-wide search) by showing `getLocalizedFullTitle()` + the source journal name instead of `getLocalizedTitle()`. Miss these and a multi-journal search links to the wrong journal and shows galleys it meant to hide.

`source` (the old 3.3/3.4 approach, for reference only): had to be host+URI, unescaped.

**A theme that ships its own CSS (`removeStyle('stylesheet')`) must bridge the core DOM of pages it does NOT override** — they still render through your header/footer but with core markup. Verified gaps that look broken without bridge rules (page wrappers are `.page_<op>`):
- **Issue archive** (`issueArchive.tpl`, `.page_issue_archive`) lists issues as `<ul class="issues_archive"><li>{issue_summary}</li>` — needs a grid + `list-style:none` or you get a single-column bullet stack. To **group by volume** (a common request — there is no separate "volume" page), the `$issues` array is newest-first so volumes are contiguous: track `$issue->getVolume()`, and when it changes close the open `<ul>` and emit a `<h2>Volume N (Year)</h2>` + new `<ul>` (open the first `<ul>` on the first non-matching item, close on `$smarty.foreach.x.last`). Grouping is per-page (the archive is paginated), which is fine.
- **Editorial masthead / history** (`editorialMasthead.tpl` `.page_masthead`, `editorialHistory.tpl`) render contributors as `<ul class="user_listing">` with `.name`/`.affiliation`/`.orcid`/`.date_start` — **zero** default styling; bridge to a card grid.
- **Announcements list** (`announcements.tpl` `.page_announcements`) renders core `.obj_announcement_summary` (+`_image`, `.date`, `.summary`) — your homepage's bespoke announcement markup does **not** apply here.
- **Category browse** (`catalogCategory.tpl`) wraps articles in `<ul class="cmp_article_list">` — needs the list reset.
- **Contact** (`.page_contact`) → `.contact_section > .address / .contact.primary / .contact.support` with `.name/.title/.affiliation/.phone .label|.value/.email`; **search** (`.page_search`) → `.cmp_form`, `button.submit`, `.search_advanced`, `<ul class="search_results">`, `.cmp_pagination`; **login/register** are `userLogin.tpl`/`userRegister.tpl` (`.cmp_form.login`).
- **Shared components** included across many pages (and by your own object templates): `notification.tpl` emits `<div class="cmp_notification {warning|error|notice}">` — style the base **and** the variants, or the issue-preview banner, "no current issue", and search errors render as unstyled plain text (styling only `.notice` misses `warning`/`error`). `breadcrumbs.tpl` is `<nav class="cmp_breadcrumbs"><ol><li><a>…</a><span class="separator">…</span></li><li class="current"><span aria-current>…</span></li></ol>`. `pagination.tpl` → `.cmp_pagination` with `.prev/.current/.next`. The announcements **list** wraps items in `<ul class="cmp_announcements">` (needs a list reset).
- **Complete frontend template inventory (3.5)** so a "ship-everything" theme misses nothing. `pkp-lib/templates/frontend/objects/`: `announcement_full`, `announcement_summary`, `announcements_list`. `ojs/…/objects/`: `article_details`, `article_summary`, `galley_link`, `issue_summary`, `issue_toc`. `pkp-lib/…/components/`: `header`, `headerHead`, `footer`, `breadcrumbs`(+`_announcement`,`_catalog`), `navigationMenu` + `navigationMenus/dashboardMenuItem`, `highlights`, `pagination`, `editLink`, `registrationForm`, `registrationFormContexts`, `announcements`. `ojs/…/components/`: `primaryNavMenu`, `breadcrumbs_article`, `breadcrumbs_issue`, `notification`, `skipLinks`, `subscriptionContact`.
- **Nav menus:** `{load_menu name="primary"}` renders `navigationMenu.tpl` → `<ul class="pkp_nav_list"><li><a>…</a><ul>…children…</ul></li>` when a custom menu is assigned, else falls back to `primaryNavMenu.tpl` (`<ul class="pkp_navigation_primary pkp_nav_list">` with an *About* dropdown). Style structurally (`.nav ul`, `.nav ul ul`) so both forms work.
- **Registration is two components, not one cmp_form layout:** `registrationForm.tpl` (fieldsets `.identity`/`.login`, each `.fields > div.given_name|.family_name|.affiliation|.country|.email|.username|.password`, every field `<label><span class="label">…<span class="required">*</span></span><input></label>`) and, on the **site-wide** register (no `$currentContext`), `registrationFormContexts.tpl` — a `<ul class="contexts"><li class="context"><div class="name"><fieldset class="roles"><label><input type=checkbox> role</label>…` role picker. Style `.fields .label`, the wrapping `<label>`, checkbox `<label>`s (flex, `input{width:auto}`), and `.contexts .context` as cards, or registration looks raw. **`editLink.tpl`** emits a manager-only `<a class="cmp_edit_link">` next to section titles on about/contact/submissions/information — give it a small badge style.

**3.5 page-template names & locations** (so you grep the right file): app-level in `pkp/ojs/templates/frontend/pages/` = `indexJournal`, `indexSite`, `article`, `issue`, `issueArchive`, `search`, `catalogCategory`, `submissions`; shared in `pkp/pkp-lib/templates/frontend/pages/` = `about`, `contact`, `announcements`/`announcement`, `editorialMasthead`, `editorialHistory`, `information`, `privacy`, `error`, `userLogin`, `userRegister`, `userLostPassword`. The "Editorial Team" page became **`editorialMasthead`** in 3.4+. None of `article`/`issue`/`search` set `$isFullWidth`, so a bespoke object template carrying its own sidebar coexists with the block-plugin sidebar. If your `article_details` has its **own** two-column grid (main + details sidebar) and you want that to be the *only* layout (no cramped third column), **override `article.tpl` and `{assign var="isFullWidth" value=true}` before the header include** — that suppresses the outer block sidebar so the article's grid is exactly two columns. (Trade-off: block plugins won't show on the article page.) There is no "volume" page in OJS — volume/number/year are the *issue* identification (`$issueIdentification`); the single-issue landing is `issue.tpl` (h1 + `issue_toc`).

**You cannot turn the theme-settings form into tabs from the theme.** Theme options render in OJS's core **Appearance → Theme** `PKPThemeForm`, which puts every `addOption` field in one flat default group — a plugin can't add tabs/pages/section headers there (it's core Vue UI). The lever you *do* have is **more options**: colours (`FieldColor`), selects (`FieldOptions` radio), text/number (`FieldText`), textareas, rich text, multilingual text — group them logically by registration order and clear labels. For deeper layout control, ship CSS variables + a `customCss` escape hatch rather than trying to re-skin the admin form.

**When you DO override a page template, copy the fragile bits verbatim and only reskin the shell.** `search.tpl` is the prime example: keep `{html_select_date_a11y …}`, the `{capture name="searchFormUrl"}…|parse_url|parse_str` dance, `{iterate from=results}`, `{page_info}`/`{page_links …}` (with every `dateFrom*`/`dateTo*` arg) and both `Templates::Search::*` hooks exactly as core has them — wrap only the surrounding markup. Same for `issueArchive` (`{include …/pagination.tpl}` + prev/next URL capture) and `contact` (`{mailto encode='javascript'}`). For **auth pages** (`userLogin`/`userRegister`/`userLostPassword`) skip the override entirely — their forms are long and fragile (csrf, reCAPTCHA/altcha, many fields). Brand them with **CSS only**, keyed off the `.page_login`/`.page_register`/`.page_lostPassword` wrappers: centre the `.cmp_form` in a narrow card. Single-announcement detail renders through `frontend/objects/announcement_full.tpl` (override the object, not just the thin page).

**The `core:` template prefix bypasses theme overrides — watch for it.** OJS's app-level `submissions.tpl` does `{capture …}{/capture}` then `{include file="core:frontend/pages/submissions.tpl"}`; the `core:` prefix forces the pkp-lib copy, *ignoring* your theme's same-named file. So putting `templates/frontend/pages/submissions.tpl` in your theme does NOT reskin the rendered output — your file replaces the thin app wrapper, but the `core:` include still pulls the unstyled pkp-lib body. To actually reskin such a page, **inline** the core body in your override (re-implement its sections) rather than re-including `core:`. For `submissions` that means rebuilding the submission-actions notice, author guidelines, preparation checklist, per-section policies (`$sections` → `getLocalizedPolicy()`), copyright notice and privacy statement, each with its `editLink`. Grep core templates for `file="core:` to find every page that uses this pattern.

**Theme-added SEO / Open Graph meta.** Core emits article `citation_*` (Google Scholar) + Dublin Core meta via indexing plugins through `{load_header}` (in `headerHead.tpl`) and the `Templates::Article::Main` hook — preserve both. Add journal-level OG/Twitter/description yourself from the `TemplateManager::display` hook (fires before `headerHead` renders), keyed so they don't duplicate:
```php
$templateMgr->addHeader('ogTitle', '<meta property="og:title" content="'.htmlspecialchars($name).'">');
```
Core ships **no schema.org JSON-LD** — adding it is a real rich-results win for a scholarly site. From the same `TemplateManager::display` hook, branch on `$template`: emit a `ScholarlyArticle` graph on `frontend/pages/article.tpl`, a `Periodical` graph on `frontend/pages/indexJournal.tpl`, and a **`PublicationIssue`** graph on `frontend/pages/issue.tpl` (`issueNumber`=`$issue->getNumber()`, `datePublished`=`substr($issue->getDatePublished(),0,10)`, `name`=`$issue->getIssueIdentification()` tag-stripped, `image`=`getLocalizedCoverImageUrl()`, DOI → `identifier`/`sameAs`; nest `isPartOf` as `PublicationVolume`(`volumeNumber`=`getVolume()`) → `Periodical` when a volume exists, else straight to `Periodical`). Pull the objects from the already-assigned template vars — `$templateMgr->getTemplateVars('publication')` / `'article'` / `'issue'` (the handler assigns them before display). Build a plain array (headline, author[] with affiliation+ORCID `identifier`, datePublished, abstract, keywords, publisher, `identifier`/`sameAs` = the DOI resolving URL). For richer indexing, **nest `isPartOf` as `PublicationIssue` → `PublicationVolume` → `Periodical`** (issue's `issueNumber`/`datePublished`, volume's `volumeNumber`, journal name+ISSN) instead of a flat Periodical — pull the issue from `getTemplateVars('issue')` and only add the volume layer when `getVolume()` is set. Add **`pagination`** from `$publication->getData('pages')`, and when it's a clean `"12-24"` range also emit numeric `pageStart`/`pageEnd` (regex on `-`/en-dash/em-dash; skip for non-range values like "e1234"). Two more high-value scholarly properties: **`license`** = `$publication->getData('licenseUrl')`, and **`isAccessibleForFree: true`** when the work is open access — detect via `$context->getData('publishingMode') === APP\journal\Journal::PUBLISHING_MODE_OPEN` *or* `$publication->getData('accessStatus') === APP\submission\Submission::ARTICLE_ACCESS_OPEN` (same test as `article_summary.tpl`); Google can surface a free-to-read indicator. Add `isAccessibleForFree` to the `PublicationIssue` graph too (journal-level OA check). `json_encode($d, JSON_UNESCAPED_SLASHES|JSON_UNESCAPED_UNICODE)`, then `str_replace('</','<\/',$json)` (can't break out of `<script>`) and `addHeader('jsonLd', '<script type="application/ld+json">'.$json.'</script>')`. Wrap it all in try/catch — it's purely additive and must never fatal the page. Two cheap wins from the same hook: a self-referencing **`rel="canonical"`** with the query string stripped (`strtok($request->getCompleteUrl(), '?')`) — `getCompleteUrl()` keeps tracking/sort params, which canonical should not — and `loading="lazy" decoding="async"` on below-the-fold `<img>` in your object templates (issue covers, thumbnails, announcement/highlight images), keeping the masthead logo eager for LCP. A matching theme convenience is an **auto "How to cite"** block: core only emits a citation when the **Citation Style Language** plugin is enabled, so building a simple APA string yourself from the publication metadata (authors → `getLocalizedData('familyName'/'givenName')` initials, year from `datePublished`, title, journal `getLocalizedName()`, `$issue->getVolume()/getNumber()`, `getData('pages')`, DOI resolving URL) and rendering it with a copy button is a high-value, self-contained add (pass it as a template var from the article-template branch of the hook). Offer **BibTeX/RIS download** with no server endpoint via `<a download href="data:application/x-bibtex;charset=utf-8,{$bib|escape:'url'}">` (build the `.bib`/`.ris` strings in PHP from the same metadata; BibTeX page ranges use `--`). A **"More from this issue"** sidebar list comes from `Repo::submission()->getCollector()->filterByContextIds([$id])->filterByIssueIds([$issue->getId()])->filterByStatus([STATUS_PUBLISHED])->getMany()` (exclude the current `$article->getId()`, cap ~5). Add a **`BreadcrumbList`** JSON-LD too — Home → Issue → Article on article pages **and** Home → Issue on issue pages (the issue is the leaf there, `item` = the canonical current URL) — build the intermediate item URLs with `$request->getDispatcher()->url($request, PKPApplication::ROUTE_PAGE, $context->getPath(), 'issue', 'view', [$issue->getBestIssueId()])`. And surface the **issue cover** (`$issue->getLocalizedCoverImageUrl()`) as a thumbnail in the article sidebar linking back to the issue. Give the JSON-LD `publisher` Organization a **logo** `ImageObject` for knowledge-panel eligibility — build the URL from the assigned template vars `displayPageHeaderLogo['uploadName']` + `publicFilesDir` (don't hand-roll the public-files path). Enrich that same publisher Organization with **`sameAs`** (an array of the journal's social-profile URLs) for stronger entity/knowledge-panel association — reuse the theme's own social-links option (`linkList()`), but accept **both** the `"Label | url"` form and a **bare URL** line (fall back to the label when the url field is empty and the label is itself a URL), validate each with `^https?://`, and `array_unique` them; because the publisher object is shared, the `sameAs` then appears on the article, issue **and** home graphs at once. On the home `Periodical`, also add a **`description`** from `$context->getLocalizedData('description')` (tag-stripped, ~300 chars, dropped by `array_filter` when blank) — Google favours an entity description. For accessibility beyond `prefers-reduced-motion`, add **`@media (prefers-contrast: more)`** (darken muted text/lines, underline links) and **`@media (forced-colors: active)`** (give buttons/cards `1px solid CanvasText`, use `Highlight` for the active nav/progress bar, hide decorative watermarks) — Windows High-Contrast users otherwise lose your borders. On the journal home, emit a **`WebSite` + `SearchAction`** node (in a `@graph` alongside the `Periodical`) so Google can show a sitelinks search box — `urlTemplate` = the OJS search URL (`$dispatcher->url(..., 'search', 'search')`) + `?query={search_term_string}`, with `'query-input' => 'required name=search_term_string'`.

**Social-card image + per-page OG (extends the JSON-LD block above).** Give the `ScholarlyArticle` JSON-LD an **`image`** and make `og:image`/`twitter:image` resolve to the real artwork, with a three-step fallback: the **article's own cover** (`$publication->getLocalizedData('coverImage')` is a `['uploadName'=>…]` array when set → `$publication->getLocalizedCoverImageUrl($context->getId())`), else the **issue cover** (`$templateMgr->getTemplateVars('issue')?->getLocalizedCoverImageUrl()`, no contextId arg), else the journal-level `seoImage` option. Do the same probe in `addSeoMeta()` so article/issue pages get a `summary_large_image` Twitter card and the cover as `og:image`; on those pages also switch `og:type` to `article` and set `og:title`/`twitter:title` to the **paper's** title (`$pub->getLocalizedTitle()`) instead of the journal name (guard with `method_exists`/`getTemplateVars('publication')` since the same method runs on every page). Give **issue pages** their own `og:title` too (issue identification + journal name) — but **gate it on `$template === 'frontend/pages/issue.tpl'`**, not just on `getTemplateVars('issue')` being set: the **journal homepage also assigns `$issue`** (the current issue), so an un-gated check would wrongly retitle the home card. (The same method runs on every frontend page, so thread `$template` into your `addSeoMeta()` helper.) For a **multilingual journal**, emit **`hreflang` alternates + `og:locale`** so Google serves the right language variant and dedupes translations: reuse the locale-switcher logic (a helper that, per supported locale, builds *this same page's* URL via the router's `urlLocaleForPage:` arg — 3.5 embeds the locale in the path) to emit `<link rel="alternate" hreflang="{code with _→-}" href="{url}">` for each locale, an `hreflang="x-default"` pointing at the `getPrimaryLocale()` URL, `og:locale` = current code and `og:locale:alternate` for the rest. Only when 2+ supported locales exist. Once `og:type=article`, emit the **article-specific OG properties** scrapers expect: `article:published_time` (`$pub->getData('datePublished')`), `article:section` (`getTemplateVars('section')->getLocalizedTitle()`), and one `article:tag` per keyword (cap ~10; keywords are objects/arrays with a `name` — handle `is_object`/`is_array`/string defensively, same as the JSON-LD branch). All purely additive.

**Overriding `galley_link.tpl` (the one core *object* worth re-skinning).** A child-of-default theme inherits every non-overridden template, so the download/view buttons render through the parent's `frontend/objects/galley_link.tpl`. To brand them (file-type icon + button styling) **copy the core template verbatim and reskin only the markup** — its logic is fragile: `$journalOverride` reassigns `$currentJournal`; `$galley->isPdfGalley()` → `pdf`/`file` type; the issue-vs-article `$page`/`$path` computation includes the **versioned** path (`…|to_array:"version":$publication->getId():…` when `$publication->getId() !== $parent->getData('currentPublicationId')`); the `$restricted` flag derives from `$hasAccess`/`$restrictOnlyPdf`; and the screen-reader subscription/fee text + `purchase_cost` (`reader.purchasePrice`) must stay. Keep the core classes (`obj_galley_link` / `obj_galley_link_supplementary` / `pdf` / `restricted`) so parent CSS and galley JS still match; add your own (`galley-btn`, `.galley-ico` SVG). Pick the icon off the same `$type` var (pdf vs generic download). **CRITICAL: `galley_link.tpl` is shared between article galleys and issue (full-issue) galleys — `$parent` is an `Article` *or* an `Issue` — so only call methods that exist on BOTH galley classes.** `ArticleGalley` (a `Representation`) has `getRemoteUrl()`, but **`IssueGalley` does NOT** (it extends `IssueFile`, whose method set is `isPdfGalley`/`getGalleyLabel`/`getBestGalleyId`/`getFile`/`getStoredPubId`/`getData`…). Calling `$galley->getRemoteUrl()` in the template fatals with *Call to undefined method* the moment an issue has a full-issue galley — and since the **homepage** TOC is included with `hideCover=true` (which skips the issue-galleys block) while the **dedicated issue page** renders it, the symptom is "only the issue page is broken," nothing else. Use the generic data accessor `$galley->getData('urlRemote')` instead (works on every galley type; it's exactly what core `article_summary.tpl` uses to detect remote galleys). Safe shared-template calls: `isPdfGalley()`, `getGalleyLabel()`, `getBestGalleyId()`, `getData(...)`. No MIME-substring matching in Smarty (no safe `strstr` modifier under OJS's security policy). No new locale keys needed — it reuses core `reader.*` keys. Also **wrap any always-on `addHeader` SEO/meta helper in `try/catch`** (like the JSON-LD ones) so a single bad accessor on one page type can't white-screen it.

**Overriding `article_details` is Google-Scholar-safe.** The `citation_*` (Highwire/Scholar) and Dublin Core `<meta>` are **not** in `templates/frontend/objects/article_details.tpl` — they're emitted into `<head>` by the core article handler / indexing plugins via `addHeader` + `{load_header}`. So a fully bespoke `article_details.tpl` (body only) does **not** drop Scholar indexing; just keep the `Templates::Article::Main` and `Templates::Article::Details` hooks for plugins (altmetrics, Paperbuzz, usageStats, CSL "How to cite") to inject. Full app-level page list to know what's inheritable vs. overridden: `aboutThisPublishingSystem, article, catalogCategory, indexJournal, indexSite, issue, issueArchive, search, submissions` **plus** the subscription set `purchaseIndividualSubscription, purchaseInstitutionalSubscription, subscriptions, userSubscriptions` — the latter only render for paid/subscription journals, so an OA theme can safely leave them on the parent and document it rather than building them.

**Custom CSS escape hatch.** Expose a `customCss` `FieldTextarea`, inject inline with high priority so it wins: `$this->addStyle('customCss', $css, ['inline'=>true, 'priority'=>999])`. Strip `</style>`/`<script>` defensively. Inline styles emit in priority then registration order (`ksort`); default priority is `STYLE_SEQUENCE_NORMAL` (10), so 999 = last. See rest-api/architecture refs for the data-layer 3.5 gotchas a theme also hits: Announcement is Eloquent (`->datePosted->format($dateFormatShort)`, `getLocalizedData('title')`, `->id`), keywords are objects (`$keyword.name`), affiliations are entities (`$author->getAffiliations()`), DOI display uses `getData('doiObject')->getData('resolvingUrl')`, and `article_details` must use the **viewed** `$publication` (not `getCurrentPublication()`) or old-version views show the current version.

**Stylesheet packaging gotchas (from a full theme audit).**
- **`htmlGalley` context CSS is isolated.** A stylesheet registered with `addStyle('x','htmlGalley.css',['contexts'=>'htmlGalley'])` is applied inside the inline HTML full-text galley, which renders in its **own** document — it does **not** inherit the main frontend stylesheet or its CSS custom properties. Make it self-contained: literal colours/fonts, **no `var(--…)`** (they resolve to nothing there). Mirror your palette as literal hex. Give it its **own `@media print`** block too (the galley prints as its own document, so the main theme's print rules don't apply): black text, `page-break-inside:avoid` on `figure`/`table`/`pre`/`blockquote`, `orphans`/`widows`, expand `a[href^="http"]::after{content:" (" attr(href) ")"}`, and make tables ink-friendly (drop the coloured `th` fill + zebra striping, use plain borders).
- **Webfonts: `<link>` in `<head>` for the main theme, `@import` only for the isolated galley.** An `@import url('fonts.googleapis…')` at the top of `theme.css` is a *chained* request — the browser must download and parse `theme.css` before it even discovers the font URL, so first paint waits on two serial round-trips. Load them in parallel instead: emit `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` **and** `<link rel="stylesheet" href="…css2?family=…&amp;display=swap">` via `addHeader` from the `TemplateManager::display` hook (gate on `str_starts_with($template,'frontend/')`, HTML-encode the `&` as `&amp;`), and delete the `@import` from `theme.css`. **Exception:** the `htmlGalley.css` document has no head you can `addHeader` into, so the webfont **`@import` is correct there** — keep it (plus a self-contained literal font stack with system fallbacks and `:lang(ar),[dir="rtl"]{font-family:"Amiri",…}` so the reading view matches site typography). Always include `&display=swap` to avoid FOIT.
- **Don't lazy-load the above-the-fold LCP image — prioritise it.** A reflexive `loading="lazy" decoding="async"` on *every* `<img>` is right for below-the-fold covers/thumbnails but is a Core Web Vitals anti-pattern on the page's **Largest Contentful Paint** image: the browser defers it, so LCP measurably regresses (Lighthouse flags "LCP image was lazily loaded"). Identify the top-of-page image per template and give it `fetchpriority="high"` (and **no** `loading="lazy"`) instead: the **masthead logo** in `header.tpl` (the LCP on interior article/issue pages), and on the journal home the **first** highlight card image (`{foreach … name=hl}…{if $smarty.foreach.hl.first} fetchpriority="high"{else} loading="lazy"{/if}`). The homepage current-issue hero cover is the LCP **only when no highlights sit above it**, so gate it: `{if $highlights && $highlights->count()} loading="lazy"{else} fetchpriority="high"{/if}`. Keep `decoding="async"` on all of them. (CLS is separately handled by giving the cover frame a fixed height — e.g. `.issue-banner .cover{height:264px}` with `img{width:100%;height:100%;object-fit:cover}` — so the image needs no explicit `width`/`height` to reserve space.) Note: a `<link rel="preload" as="image">` is stronger still but risks a wasted double-fetch if the preloaded URL doesn't byte-match the rendered `src`; `fetchpriority` on the `<img>` is the safe default.
- **Two cheap, safe perf/SEO wins.** (a) **`noindex` internal search results** — emit `<meta name="robots" content="noindex, follow">` (via `addHeader`) when `$template === 'frontend/pages/search.tpl'`. Google explicitly recommends keeping site-search result pages out of the index (thin/duplicate content); `follow` still lets it crawl the article links found there. (b) **`content-visibility:auto` on long off-screen lists** (reference lists `.ref-list li`, archive cards `.issues_archive > li`) skips rendering them until scrolled near — a free render-perf win; pair with `contain-intrinsic-size:auto <px>` (the `auto` keyword remembers each item's real height so the scrollbar doesn't jump). Do NOT put it on elements that also run a scroll-reveal IntersectionObserver (e.g. homepage article rows) — the two rendering controls fight; keep content-visibility on non-animated pages (article references, archive).
- **Dark mode for the HTML full-text galley — it can only follow the OS.** The galley renders as its OWN document via `htmlGalley.css` (isolated context, no site `data-theme` attribute, no theme JS), so the site's dark toggle can't reach it. The one signal available is `@media (prefers-color-scheme: dark)` — add a block to `htmlGalley.css` that remaps the literal colours (dark canvas + light text, dark table header/zebra/borders, lighter links, brass blockquote rule). Put it BEFORE the `@media print` block so print rules still win on paper. This is the only way to give the reading view a dark mode; it tracks the OS, not the on-site toggle.
- **A web-font privacy/performance toggle.** Wrap the `addHeader` preconnect + Google-Fonts `<link>` emission in `if ($this->getOption('loadWebFonts'))` (default on). When an admin disables it (GDPR/privacy, or perf), the font stacks fall back to the Georgia/system-serif tail already in every `--serif`/`--body` stack — zero external requests, no layout change beyond the typeface. Caveat to document: the isolated `htmlGalley.css` loads its fonts via a static `@import` you can't gate from PHP, so the toggle governs the main site; the galley keeps its self-contained fonts.
- **Style login / register / lost-password via CSS — do NOT override the templates.** Those pages carry fragile auth logic (CSRF token, reCAPTCHA/Altcha, remember-me, source param, register's many fields + privacy consent + reviewer roles, implicit-auth). A bespoke rewrite risks breaking sign-in/registration — the highest-stakes failure on the site. Instead leave the core `userLogin.tpl`/`userRegister.tpl` to render and target their stable class names from `theme.css` (verified in pkp-lib `stable-3_5_0`): page wrappers `.page_login` / `.page_register` / `.page_lostPassword`; forms `form.cmp_form.login#login` / `form.cmp_form.register#register`; login fields `.username` / `.password` / `.remember.checkbox`, links `a.register` (on login) and `a.login` (on register), `button.submit`, `fieldset.recaptcha_wrapper`; register sections `.fields`, `fieldset.consent`, `.optin.optin-privacy` / `.optin-email`, `fieldset.reviewer`, `#reviewerInterests`, `fieldset.altcha_wrapper`, `.buttons`. A premium-but-safe look: center the page (`.page_login{max-width:440px;margin-inline:auto}`, register wider ~640px for its 2-col identity grid), make the form a card with a **brass top accent** (`border-top:3px solid var(--brass-500)`) + shadow, center the page `h1`, give login/lost a **full-width `button.submit`**, and center the `.recaptcha_wrapper`. All palette-var-driven so dark mode + RTL come free. (Body classes `pkp_page_login` / `pkp_op_register` are also available for page-scoped rules.)
- **Search page polish (the one auth-adjacent template a theme DOES own and can reskin).** Make the main query input a hero (`input.query{font-size:1.08rem;padding:.75rem .95rem;width:100%}`), lay the `.date_range` from/to side-by-side (`display:flex;gap:1rem` with `> div{flex:1 1 200px}`), and wrap the author + journal filters in a 2-col `.search-filter-grid` (collapsing to 1col under ~560px). Keep the `{block name=searchQuery}`/`{block name=searchAuthors}` blocks and the `Templates::Search::SearchResults::*` hooks intact when restructuring.
- **Optional inline PDF reader on the article page — ship it default-OFF.** Embedding the full-text PDF on the abstract page is a premium touch, but make it an opt-in option (`showInlinePdf`, default 0) because some servers send `X-Frame-Options`/CSP `frame-ancestors` that block the `<iframe>`, and you can't detect that server-side. In `article_details.tpl`, find the first `$primaryGalleys` entry where `$galley->isPdfGalley()`, gate it on the **current** publication (`$currentPublication->getId() === $publication->getId()`) to dodge the versioned-download-path complexity, build the URL with `{url page="article" op="download" path=$article->getBestId()|to_array:$galley->getBestGalleyId()}`, and embed `<iframe src="…#view=FitH" loading="lazy" title="…">`. ALWAYS keep the existing download buttons and add a visible fallback link below the frame (`<a href="{same url}">Download PDF</a>`) so a blocked embed still gives the reader the file. CSS: `width:100%;height:80vh;min-height:480px` (drop to ~70vh on mobile) and `@media print{.pdf-embed{display:none}}`. Place it after the abstract/keywords but before `{call_hook name="Templates::Article::Main"}` so usage-stats/altmetric plugins still inject after it.
- **Contact page polish.** The core `contact.tpl` exposes `$mailingAddress`, `$contactName/Title/Affiliation/Phone/Email`, `$supportName/Phone/Email`. Make phone numbers **click-to-call** (`<a href="tel:{$contactPhone|escape}">`) — a real mobile win — and give each contact block heading a small inline SVG icon (map-pin / person / life-ring) coloured `var(--brass-600)` (dark: `--brass-400`). Keep `{mailto address=$email encode='javascript'}` for the obfuscated email link. The blocks already grid via `.page_contact .contact_section{grid-template-columns:repeat(auto-fit,minmax(240px,1fr))}`.
- **"Media upload" for header/footer/sidebar — theme options can't take file uploads, so use image-URL fields.** `ThemePlugin::addOption()` rejects `FieldUpload`/`FieldUploadImage` (theme settings are saved as scalar plugin settings, not files — confirmed: no core theme offers an upload option). The working pattern is a `FieldText` holding an image **URL**, with the description telling admins how to get one: upload the image via a rich-text editor in **Settings → Website → Appearance** (the TinyMCE there uploads to public files) and copy its address, or use any hosted URL. Good additions: a **header background image** (`mastheadBgImage`), a **footer logo** override (`footerLogo`), and a **sidebar banner** (`sidebarImage` + `sidebarImageLink` + multilingual `sidebarImageAlt`) for calls-for-papers. **Security: an admin URL going into CSS `url()` must be sanitised** — `preg_replace('#[^A-Za-z0-9:/._\-%?=&,~+]#','',$url)` strips quotes/parens/`;`/backslash so it can't break out of the declaration (a URL in a template `src="…"` only needs `|escape`). For a header photo, layer a dark brand overlay so white masthead text stays legible: `background-image:linear-gradient(rgba(navy,.88),rgba(navy,.72)),url('…');background-size:cover`. Render the sidebar banner as a borderless `.widget` with `overflow:hidden` so the image fills the card and inherits its radius; include it in BOTH the shared `journalSidebar.tpl` and the homepage's own sidebar so it's consistent.
- **Organising a long theme-options form — OJS has no native tabs/groups for theme options** (verified: `ThemePlugin` stores options in a flat array; `getOptionsConfig()` is flat; `addOption` takes no `groupId`). When the list grows past ~20 settings it becomes an unusable scroll. The best available fix is to insert **inert section headers** using the `FieldHTML` field type (it exists in `PKP\components\forms\`, has `isInert = true` so it stores no value and never participates in save/`getOption`). Add one before each logical group: `$this->addOption('secX', 'FieldHTML', ['label' => '', 'description' => '<div style="border-top:2px solid #d9cfb4;margin:26px 0 0;padding-top:14px;font-weight:700;font-size:1.14rem;color:#0a1f3c">'.__($key).'</div>'])` — the styled `<div>` in `description` (rendered as HTML by field-html) gives a clear full-width divider regardless of the form's default field styling. A small closure keeps it DRY. This turns ~60 settings into a dozen labelled sections. (Real tabbed pages would require a core form override, not worth it.)
- **Homepage entrance animations without FOUC or motion-sickness.** Scroll-reveal is a cheap premium touch: CSS `.reveal{opacity:0;transform:translateY(18px);transition:...}` + `.reveal.in{opacity:1;transform:none}`, with an `IntersectionObserver` (in `js/alnakib.js`) adding `.in` as each element scrolls in and `unobserve`-ing it. Three rules so it never backfires: (1) **gate the hidden state behind a JS-added class** — `.alnakib-anim .reveal{opacity:0}` and have the JS add `.alnakib-anim` to `<html>` only after DOM-ready, so a non-JS visitor (or a crawler) never sees permanently-invisible content. (2) **Respect reduced motion** — skip the whole thing when `matchMedia('(prefers-reduced-motion: reduce)').matches`, and add a `@media(prefers-reduced-motion:reduce){.alnakib-anim .reveal{opacity:1!important;transform:none!important}}` belt-and-suspenders. (3) **Scope to the homepage** via `document.body.classList.contains('pkp_page_index')` (OJS sets `pkp_page_{requestedPage}` on `<body>`). Stagger repeated items with a tiny per-index `style.transitionDelay` (cap ~6). A delightful extra: **count-up** numeric stat values when the stats row reveals — regex `^(\d[\d,]*)(\D*)$` so it animates `1,250` / `21%` but safely no-ops on token/text values, eased with `requestAnimationFrame` (browser JS — `Date.now`/rAF are all fine here, unlike workflow scripts).
- **Relative `url()` in your own theme CSS resolves relative to the CSS file, not the page.** With `theme.css` at the plugin root and an `assets/` sibling, `url('assets/logo.png')` works from any page URL. (In a `<style>`-injected/inline block there is no base file, so use absolute `{$baseUrl}/…` there instead.)
- **Palette-from-one-colour via `color-mix()`.** Derive a whole scale from a few brand-colour options (`--navy-900:color-mix(in srgb, {$primary} 66%, #000)` …), and only emit the `:root{…}` override block when the option **differs from its default** — so the shipped defaults stay byte-identical to the reference design and you're not fighting your own cascade.
- **One-click "design presets" (themes-within-the-theme).** Admins love a single dropdown that restyles the whole site. Implement it as a `preset` `FieldOptions` radio (default `custom`) plus a `presetValues()` helper returning a coordinated map per preset — base `primary`/`accent`/`paper` colours + `corner`/`masthead`/`hero` style. Then make the preset OVERRIDE the individual options by reading effective values with null-coalescing: `$preset = $this->presetValues(); $primary = $preset['primary'] ?? $this->getOption('primaryColour');` — `??` safely yields the option when `$preset` is null (the `custom` case), so the existing per-option emission logic is untouched; the preset just feeds it different inputs. Reuse the SAME `color-mix()` scale derivation so the whole palette shifts coherently from three base colours. Three rules: (1) make one preset **equal the shipped defaults** by reusing your `DEF_*` constants (so selecting it is pixel-identical and the `!== DEF_*` emission gate correctly emits nothing). (2) Presets must only use **fonts already loaded** (your Google-fonts `<link>` set, or system stacks) — don't pick a font a preset can't actually render. (3) A value set in `loadTemplateData` rather than `injectAppearance` (e.g. the hero style) needs the same `presetValues()['hero'] ?? getOption('heroStyle')` treatment there too, so call the helper in both methods. Document that a preset supersedes the individual colour/corner/masthead pickers (choose `Custom` to hand-tune).
- **Inline-injected CSS silently defeats your stylesheet's mobile media queries.** A theme that emits option-driven rules via `addStyle(..., ['inline'=>true])` outputs them in a `<style>` AFTER `theme.css`. So an **unconditional** injected rule (e.g. a `sidebarPosition=left` `grid-template-columns: <w> 1fr`) has equal specificity to your `@media(max-width:960px){…1fr}` collapse but **later source order — it wins even on phones**, leaving two cramped columns. Any injected rule that changes a property your stylesheet makes responsive must itself be wrapped in `@media(min-width:<breakpoint+1>){…}` matching that grid's own collapse point (and scope the `order:` swaps the same way, or a "left" sidebar lands ABOVE the content when stacked). `!important` in the stylesheet's mobile rule also defends against this (that's why one grid was already safe), but min-width-gating the injection is cleaner. Same trap applies to `customCss` and any `addHeader` `<style>`.
- **Cheap SEO/social wins worth adding once the JSON-LD/OG block exists:** `og:image:alt` + `twitter:image:alt` (reuse the page/article title you already computed for `og:title` — improves social-card accessibility), and `mainEntityOfPage` (= the query-stripped canonical URL) on the `ScholarlyArticle` graph (a Google-recommended property). Both are one-liners next to the existing image/url emission.
- **Per-page meta description — don't reuse the journal blurb on every page.** A common miss: emitting `<meta name="description">`/`og:description` from `$context->getLocalizedData('searchDescription')` site-wide, so every article shares the journal's description. Override per page: on an article use the **publication's abstract** (`$pub->getLocalizedData('abstract')`, tag-stripped, ~300 chars), on an issue use the issue description, else the journal — so each URL gets a unique, relevant SERP snippet. OJS core does NOT emit a frontend `meta description`, so the theme's is the only one (no dup).
- **Leave Google Scholar `citation_*` (Highwire) meta to the bundled Google Scholar plugin — do NOT emit it (not even a lone `citation_issn`) from the theme.** Scholar requires the COMPLETE set (citation_title/author/journal_title/publication_date/volume/issue/firstpage/lastpage/doi/issn/pdf_url) and ONLY on the article abstract/full-text page; a partial tag, or one on non-article pages, is wrong, and emitting any of it duplicates the plugin's output (duplicate citation_* confuses Scholar). The theme's job is JSON-LD + OG + hreflang + canonical; `citation_*` and `DC.*` are the Google-Scholar and Dublin-Core plugins' jobs (both bundled). When auditing indexing, verify those plugins are enabled rather than re-emitting their meta. JSON-LD nit: normalise `datePublished` to an ISO date (`substr($dp,0,10)` — `getData('datePublished')` can include a time), and give an author's ORCID both `identifier` and `sameAs` (the ORCID URL) for entity linking. Also: never emit a dead `href="#"` for an admin link that has a label but no URL (info/footer/social lists) — render the label as plain text instead, so screen-reader/keyboard users don't hit a no-op link.
- **Scalar layout options → a CSS var with a fallback, capped on mobile with `min()`.** For a single-value sizing option (logo height, content width, base font size) inject `--logo-h:<value>` into the `:root{}` block (same place as the colour vars) and consume it with the default baked into the `var()` fallback: `.brand-logo{height:var(--logo-h,72px)}`. Two wins: the shipped CSS is byte-identical when the option is unset (the fallback *is* the design), and you never need a separate "is it set?" branch in the stylesheet. The catch is **responsive shrink** — a fixed mobile override (`@media(max-width:640px){.brand-logo{height:54px}}`) would *enlarge* a logo the admin sized to 40px. Cap instead of replace: `height:min(var(--logo-h,72px),54px)` — the element follows the admin's value on desktop but is never larger than the breakpoint ceiling on small screens.
- **Optional, default-OFF feature sections (e.g. a homepage hero).** When adding a whole new editable block, ship it off by default so existing installs don't suddenly change: reuse the shared enable/disable radio helper but flip its default — `addOption('showHero','FieldOptions', array_merge($tgl(...), ['default'=>0]))`. Gate the template on **both** the toggle and real content so an enabled-but-empty section renders nothing: `{if $alnakibShowHero && ($alnakibHeroHeading || $alnakibHeroText)}`. Register the hero's heading/text in the multilingual list (per-locale EN/AR) like every other user-facing string. Place a full-width banner **outside** the homepage's own `.layout` grid but still inside `.pkp_structure_content` (which already supplies `max-width:var(--wrap)` + `padding-inline:var(--gutter)`), so it spans the content column without bespoke full-bleed math; style it from palette vars (`linear-gradient(180deg,var(--paper-2),var(--paper))`, `color:var(--primary-deep)`) so it adapts to dark mode and `[dir=rtl] .hero-heading{font-family:var(--arabic)}` for Arabic.
- **Sidebar two-column trigger: key off the rendered element, not just the flag.** The footer emits `.pkp_structure_sidebar` **only** when `Templates::Common::Sidebar` returns content, while the header adds `.has_sidebar` from `$hasSidebar`. Style the grid with `.pkp_structure_content:has(> .pkp_structure_sidebar){grid-template-columns:1fr 312px}` (default 1fr) so you never get an empty second column if the flag and content disagree; add the `.has_sidebar` selector alongside it only as a fallback for browsers without `:has()`. Both derive from the same block list, so in practice they agree.
- **If you build the locale switcher as `<a>` links** (per the 3.5 `urlLocaleForPage` approach above), style `.lang-toggle a` / `.lang-toggle a.current` — don't leave `button[aria-pressed]` rules from a 3.3-era markup that now match nothing.
- **Theme JavaScript:** register with `$this->addScript('x','js/x.js',['contexts'=>'frontend','defer'=>true])` (path is relative to the plugin root; it's emitted by `{load_script context="frontend"}` in `footer.tpl`). Best pattern for things like a mobile hamburger / back-to-top: **render the button(s) in the templates** (so their labels are translated via `{translate}`) and let the JS only wire behaviour + toggle classes — keeps it dependency-free and usable with JS off. Gate any smooth-scroll behind `matchMedia('(prefers-reduced-motion: reduce)')`. Note the default theme bundles **Swiper** for highlights; a theme that drops the default JS must not rely on Swiper-driven markup. A few cheap robustness wins for hand-rolled theme JS: **`requestAnimationFrame`-throttle** scroll/resize handlers (reading-progress bar, back-to-top visibility) so they fire once per frame, not per event; close the **mobile nav on `Escape` (return focus to the toggle) and on outside click**, not just on link-click, for keyboard/touch users; and set **`aria-hidden="true"`** on any purely-decorative element you inject from JS (a reading-progress bar) so screen readers skip it.
- **Wayfinding + heading semantics (two easily-missed gaps).** (1) **Exactly one `<h1>` per page.** The header/masthead does **not** emit one — the journal name there is a styled `<span class="jtitle">`, not a heading — so each **page** template owns its h1. Audit with `grep -L '<h1' templates/frontend/pages/*.tpl`; the multi-journal **site index (`indexSite.tpl`) is the usual miss** (its first heading is the `<h2>` journals list, `context.contexts`), which is both an a11y and an SEO defect. Add `<h1 class="pkp_screen_reader">{$siteTitle|escape}</h1>` — screen-reader-only (the class is defined by the theme/core) so it doesn't visually duplicate the masthead. (2) **OJS `{load_menu}` doesn't mark the current page.** A theme can ship `.nav a.active` / `[aria-current="page"]` styles but they never trigger, so the primary nav gives no "you are here" cue. Wire it in JS (progressive — no JS just means no highlight): compare each `.nav a[href]`'s URL **pathname** to `location.pathname` (trailing-slash-normalised), **longest match wins** so a deep page beats its parent, and gate root `"/"` to match the homepage **only** (else it matches everything). Set both `classList.add('active')` (visual) and `setAttribute('aria-current','page')` (screen readers).
- **Section labels must be real headings, not styled `<div>`s.** It's tempting to render a section label (issue-TOC section name; the article page's "References" / "Author Biographies" / "Data Availability" / "How to Cite") as `<div class="toc-section-title">` — but then it's invisible to the document outline, so a screen-reader user jumps from the page `<h1>` straight to the article `<h3>`s, skipping the level. Emit a real heading: in a shared TOC partial used at different depths, take the level as a param and output it dynamically — `<{$heading} class="toc-section-title">…</{$heading}>` (issue page passes `h2`, homepage passes `h3`); on the article page make the section labels `<h2>` siblings of the Abstract `<h2>`. Two gotchas that bite when you do this: (a) **a class scoped with the child combinator silently fails when the class is reused elsewhere** — `.toc-section > .toc-section-title` styles the TOC copy but **not** the article-page copies (they sit in plain `<section>`s), so they render as unstyled body text; use an **unscoped** base class for any label style you reuse across templates. (b) A class that may render as either a `<div>` or a heading needs `margin:0 0 1rem` (not just `margin-bottom`) so the browser's **default heading top-margin** doesn't shift the layout when you switch the element type. Apply the same to **sidebar widget headings** (`.widget-head`, `.db-fmt`) — make them `<h2>` within their `<aside>` (complementary landmark) so screen-reader users can jump between widgets. Two extra gotchas there: (1) a **styled bar heading** (one with its own `background`/`padding`/`border-radius`, like a navy widget-head) needs `margin:0` (not just `margin-top:0`) — the UA heading **bottom** margin would otherwise open a gap between the coloured bar and the widget body; (2) **name every `<aside>`** with `aria-label` (a bare `<aside>` announces only as "complementary"), and give *distinct* labels — an article page can show both the theme's article-detail sidebar **and** the block-plugin sidebar (`.pkp_structure_sidebar`) at once, so "Article information" / "Journal information" beat two generic "Sidebar"s. When a heading carries an `id` referenced by `aria-labelledby` elsewhere (e.g. a full-issue galley label), keep the `id` on the new `<h2>`. **Audit EVERY overridden core template for this, not just the TOC/article ones** — the CMS pages bite too: core's `submissions.tpl`/`about.tpl` render their section titles (Author Guidelines, Submission Checklist, per-section policies, Copyright Notice, Privacy Statement) as real `<h2>`s, so a reskin that swaps them to `<div class="toc-section-title">` silently demotes a whole page out of the outline. Grep your `templates/frontend/pages/*.tpl` for `<div class="toc-section-title"` after a reskin pass. And on the **homepage**, a prominent first-announcement card whose title is an `<h4>` sitting directly under the page `<h1>` (with the hero off, the default) is a WCAG heading-**order** skip (h1→h4) — make it `<h2>`; if a `.notice h4{…}` style was scoped to the old tag, widen the selector (`.notice h2,.notice h4`) so it doesn't lose its styling.
- **A load-time CSS entrance animation silently defeats a JS scroll-reveal on the same element.** If you ship both (a) a keyframe entrance for content blocks — `.section-head,.issue-banner,.notice,.lead-para{animation:alnakib-fade-up .55s … both}` — and (b) an IntersectionObserver scroll-reveal that hides the same elements via a JS-added class (`.alnakib-anim .reveal{opacity:0}` then unhide on intersect), the **animation wins the cascade**: a running/finished `animation` with `fill-mode:both` sets `opacity:1` at higher precedence than the normal `.reveal{opacity:0}` declaration, so the elements appear immediately and the scroll-reveal never visibly fires (worst on below-the-fold sections). Don't run both on the same node. Cleanest fix: keep the load keyframe as the entrance for **inner** pages but turn it **off where the JS owns the element** — `.alnakib-anim .section-head,.alnakib-anim .issue-banner,…{animation:none}` (the JS adds `.alnakib-anim` to the root only on the homepage), so inner pages get the keyframe and the homepage gets the scroll-reveal, never both. (General rule: `animation` + `transition` beat normal declarations for the properties they touch — if a JS state class must override an animated property, either stop the animation or set the property in a rule of equal-or-higher weight with `!important`.)
- **Audit-derived polish that recurs across OJS themes.** A full review of a mature theme turned up a consistent cluster worth checking for proactively: (1) **Escape `getIssueIdentification()`** — it's manager-set text and core escapes it everywhere (`|escape`); a bespoke theme easily prints it raw in 3-4 spots (issue card title, article sidebar "Published" issue link, homepage banner/sidebar), a stored-XSS gap. Grep `getIssueIdentification()}` for un-piped uses. (2) **Link the issue DOI, don't print the bare string** — `$issue->getDoi()` returns `10.x/y`; use `$issue->getData('doiObject')->getData('resolvingUrl')` wrapped in `<a>` (match `issue_toc.tpl`; the homepage banner often regresses to the bare form). (3) **Dark-mode "light islands"** — status boxes with *literal* light backgrounds (`.cmp_notification.warning/.error/.success`, `.pkp_form_error`/`.form_errors`, `.required` red) have no `[data-theme="dark"]` override, so they glow bright on the dark canvas; add deep-tinted-bg + light-text dark variants. (4) **RTL arrows don't auto-mirror** — a literal `→`/`←` in a "View all / Back" link always points the LTR way; wrap it in `<span class="rtl-flip">` with `[dir="rtl"] .rtl-flip{display:inline-block;transform:scaleX(-1)}` so it flips in Arabic. (5) **Sticky-nav anchor offset** — `:target{scroll-margin-top:5rem}` so in-page anchors (`#data-availability`, TOC jumps) don't hide under the sticky nav. (6) **Duplicate adjacent link** — when a cover image AND the title both link to the same issue/article, give the decorative one `aria-hidden="true" tabindex="-1"` so screen-reader/keyboard users hit it once. (7) **Copy-to-clipboard "stuck label"** — capture the button's resting label **once at init** (not inside the click handler) and `clearTimeout` the reset, or a rapid second click saves "Copied" as the label and it never reverts. (8) **The mobile wide-table `overflow-x:auto` rule must include the reading-view table classes** (`.data-table`, `.reading table`), not just `.prose/.abstract-block/.alnakib-rich table`, or full-text galley-landing tables overflow on phones. (9) **Footer column headings are real headings too** — a bespoke footer's column titles are easy to ship as `<h5>` (or a `<div>`) chosen for their small-caps *look*, but that skips heading levels after the page content. Make them `<h2>` inside the `role="contentinfo"` footer and move the styling to a `.foot-col h2` selector; since you're now inheriting the global `h2` rule, set `margin:0 0 1rem` (not just `margin-bottom`) and an explicit small `font-size`/`line-height` so the visual is unchanged. Same principle as the sidebar widget-heads.
- **Finishing the homepage-sidebar de-duplication (the refactor the consistency rule sets up).** Once `journalSidebar.tpl` is the shared partial for inner pages, collapse the homepage's hand-rolled `<aside>` body into `{include file="frontend/components/journalSidebar.tpl"}` so sidebar edits live in ONE file. Two things make it a safe swap rather than a behaviour change: (1) the partial's **Current Issue** widget must read the **globally-assigned** current issue (`$alnakibCurrentIssue`/`…Cover`, set from the `TemplateManager::display` hook on every page) — on the journal homepage that global IS the page's `$issue`, so the rendered widget is byte-identical, but the partial stays page-agnostic for the inner pages that have no `$issue`. (2) Keep the homepage's **own** `{capture}{call_hook name="Templates::Common::Sidebar"}{/capture}` render after the include: the homepage sets `isFullWidth`, so `footer.tpl` skips its sidebar slot (and the block-plugin hook it renders there) — the homepage must therefore emit that hook itself or it silently loses Custom-Block-Manager widgets. Net: the ~75-line duplicate disappears and the long-standing guard/data-source drift (homepage CTA gated without `$currentContext`; widget fed from `$issue` vs the global) is resolved in the partial's favour. Verify after: Smarty tag balance on the shrunken page, and grep the page for the literal `journalSidebar` to confirm exactly ONE `{include}` (a stray second one renders the sidebar twice).
- **`getLocalized*Title(null, 'html')` MUST be paired with `|strip_unsafe_html` — never printed raw, and never `|escape`d.** OJS does not fully sanitize publication titles/subtitles on *input*; it relies on *output* sanitization, and the `'html'` format deliberately returns the title WITH any author/editor-entered inline markup (`<i>`, `<sub>`, `<sup>`). So (a) printing it **raw** is a stored-XSS hole — a title like `<img src=x onerror=…>` executes on the public page, and anyone who can set a title (authors at submission, editors) can plant it; this is exactly the trap a bespoke theme falls into on the article-page `<h1>` while its own `article_summary.tpl`/subtitle copies correctly strip. (b) `|escape` is ALSO wrong — it double-escapes the intended `<i>` into visible `&lt;i&gt;`. The one correct idiom is `{$publication->getLocalizedTitle(null, 'html')|strip_unsafe_html}` (same for `getLocalizedSubTitle`/`getLocalizedFullTitle`); core does this everywhere. After any reskin, grep overrides for `getLocalized*Title(null, 'html')}` that lack a trailing `|strip_unsafe_html`. Two adjacent gotchas the same security pass surfaced: (1) **a dark-mode override on a GROUPED selector only protects the selectors you actually list** — `[data-theme=dark] .x, .search_advanced legend{color:var(--ink)}` leaves the *generic* `.cmp_form legend` (login/register/contact) still `navy-800` on the dark card (~1.1:1, invisible); when you fix a dark-mode "light island", enumerate EVERY light-colour selector that shares the original rule, not just the one you noticed. (2) **a separate-document galley (`htmlGalley.css`) does NOT inherit the main page's web-font gating** — a remote `@import` there fetches Google Fonts even when the site's privacy/`loadWebFonts` toggle is OFF (and is render-blocking, chained: parse CSS → discover font URL → fetch). Drop it for a system-serif fallback stack (or self-host) so the galley honours the same privacy stance as the rest of the theme.
- **OJS 3.5 ORCID display — what the real API is, and "verify before you rip it out".** Author ORCID lives in a **trait**, so `hasVerifiedOrcid()` and `getOrcidDisplayValue()` are NOT in `Author.php` itself — reading the class file alone wrongly concludes "method absent", but `method_exists()` returns true and core's own `article_details.tpl`/`editorialMasthead.tpl` call them, so they're real. The underlying schema field is **`orcidIsVerified`** (boolean) — `$author->getData('orcidIsVerified')` is the dependency-free check. Core assigns the icon markup as template vars **`$orcidIcon`** (authenticated/green) and **`$orcidUnauthenticatedIcon`** (grey) on the article + masthead pages (from `OrcidManager::getIcon()`/`getUnauthenticatedIcon()`), so a child theme printing `{$orcidIcon}` already works there — but they're NOT guaranteed on arbitrary pages, so a self-contained theme can inline the official ORCID iD SVG itself (circle `fill:currentColor` so one markup serves both states: green `#A6CE39` when verified, grey otherwise — and per ORCID brand guidelines never show the green logo for an *unauthenticated* iD). Editorial-masthead reviewers are gated differently (`getData('orcidAccessToken')`), matching core. General lesson: a research/audit subagent's "method X doesn't exist in 3.5" is often a **false positive when X lives in a trait or parent class** — confirm against where core actually *calls* it before changing anything.
- **A "Recent articles" sidebar block is a cheap, high-value reader feature via the submission Collector.** `Repo::submission()->getCollector()->filterByContextIds([$id])->filterByStatus([PKPSubmission::STATUS_PUBLISHED])->orderBy('datePublished','DESC')->limit(6)->getMany()` — the `orderBy` args ARE the `Collector::ORDERBY_DATE_PUBLISHED` / `ORDER_DIR_DESC` string values (`'datePublished'`/`'DESC'`), so passing the literals avoids a namespace import. Build it ONCE in the `TemplateManager::display` hook's `frontend/` branch (that hook fires once per page for the top-level template — one bounded query, NOT per-`{include}`), assign a global array (`[['id'=>getBestId(),'title'=>getLocalizedTitle(null,'html'),'date'=>getData('datePublished')],…]`), and render it from the shared `journalSidebar.tpl` so it appears site-wide; gate it on an admin option and on `$currentContext` existing (skip the multi-journal site index, which has none). Print the title with `|strip_unsafe_html` (it's the `'html'` format) and format the date with `|date_format:$dateFormatShort`; exclude the article currently being viewed via its `getId()` (read `getTemplateVars('article')` in the hook — present on article pages, null elsewhere). Wrap the query in `try/catch (\Throwable)` returning `[]` so a DB hiccup never white-screens every page. (Note the query still runs on `isFullWidth` pages whose template doesn't render the shared sidebar — a minor waste; gate harder only if it matters.)
- **Previous/next issue navigation is a cheap, expected reader feature OJS omits from the frontend.** On the issue landing page (`frontend/pages/issue.tpl`), add a pager to the adjacent published issues. The OJS **issue** Collector is app-level (`APP\issue\Collector`, NOT pkp-lib): `Repo::issue()->getCollector()->filterByContextIds([$id])->filterByPublished(true)->orderBy('datePublished')->getMany()` returns a `LazyCollection` **newest-first** (the `orderBy` constant `ORDERBY_DATE_PUBLISHED='datePublished'` maps to DESC, and it takes ONE arg — unlike the submission Collector's two-arg `orderBy(sorter,dir)`). Find neighbours by iterating and tracking the previous item: when you hit the current issue's `getId()`, the item before it is the **newer** neighbour and the next item is the **older** one — then `break`, so a `LazyCollection` only streams rows up to current+1 (no full-archive scan). Assign `['id'=>getBestIssueId(),'label'=>getIssueIdentification()]` (escape the label in the template) for each, gate the whole pager on either existing, and render older on the start side / newer on the end side with `.rtl-flip` chevrons so it mirrors in Arabic. Build it once in the `TemplateManager::display` hook gated on the issue template (covers both `issue/view` and `issue/current`). Keep it option-free — it's a universal nav aid, and the user disliked option bloat ([[settings-ux-tabs-preference]] equivalent).
- **Bilingual journals: show ALL abstract languages on the article page, each in its own direction/font.** OJS stores the abstract as a locale-keyed array, but a theme typically renders only `getLocalizedData('abstract')` (one language). For a bi/multilingual journal, present the others too (the English abstract of an Arabic article and vice-versa) — high value, and trivially additive: keep the existing primary block untouched and APPEND the rest. In PHP, read `$publication->getData('abstract')` (the full array), and for each non-empty locale **whose HTML differs from `getLocalizedData('abstract')`** (dedupe by content, NOT by guessing which locale getLocalizedData picked — its fallback chain is current→primary→… and replicating it is fragile), emit `['locale','name'=>\Locale::getDisplayLanguage($loc,$loc) endonym,'dir'=>rtl-set?,'html']`. In the template render each as `<section class="abstract-block abstract-alt" lang="{loc}" dir="{dir}"><h2>Abstract · {name}</h2>{html|strip_unsafe_html}</section>`. Crucial CSS for **mixed-direction** content: set the font by the section's OWN dir with attribute selectors — `.abstract-block[dir="rtl"]{font-family:var(--arabic)}` AND `.abstract-block[dir="ltr"]{font-family:var(--body)}` (+ the matching `h2` overrides) — otherwise an English abstract inside an RTL page (or Arabic inside an LTR page) inherits the wrong font from the ancestor-keyed `[dir="rtl"] …` rules. The primary block keeps no `dir` attr so it follows the page locale as before. RTL locale set for `dir`: `ar,fa,he,ur,ps,sd,ug,yi,dv` (match on the 2-letter prefix of the OJS locale code).
- **Register/login (`cmp_form`) dark-mode labels: the field labels need a HIGHER-specificity dark override than the generic one.** The OJS registration form (`<form class="cmp_form register">`) renders field labels as `<span class="label">` inside `.fields`, so the live rule is `.pkp_structure_main .cmp_form .fields .label{color:navy}` — a theme's dark override on `.cmp_form .label` ties it on specificity and LOSES on source order (the `.fields .label` rule is defined later), leaving Given Name / Email / Password / "Reviewing interests" dark-on-dark. Fix with `[data-theme="dark"] .pkp_structure_main .cmp_form .fields .label{color:var(--ink)}` (the extra `.fields` class outweighs it). Two more dark-mode traps on the same form: the privacy/email **consent text is a bare `<span>` inside `.optin label`** (no `.label` class) so target `[data-theme="dark"] … .cmp_form .optin span` (or just set the whole form's base text: `[data-theme="dark"] … .cmp_form{color:var(--ink)}`), and **fieldset `<legend>`s** (`fieldset.consent`/`.reviewer`, legends "Privacy Options"/"Login"/reviewer prompt) are `fieldset > legend` (specificity `0,2,2`) so a bare-`legend` override won't reach them — include `fieldset > legend` in the selector. Setting `[data-theme="dark"] .pkp_structure_main .cmp_form{color:var(--ink)}` as a base catches every descendant text node and is the most robust single fix. **Modern auth design (CSS-only, no template override):** the real hooks are `fieldset.consent` / `fieldset.reviewer` / `.optin` (opt-in rows) / `.reviewer_interests` / `.recaptcha_wrapper`; divide register `fieldset`s with bottom borders (last-of-type none), restyle `fieldset > legend` as small-caps section headers (must use `fieldset > legend` to beat core's `0,2,2` rule), wrap the consent/reviewer fieldsets in a `var(--paper-2)` soft card, flex the `.optin label` (checkbox + text), and make the submit full-width — all scoped to `.page_register`/`.page_login` so other `cmp_form`s are untouched.
- **Auto-populate the homepage statistics row from live data (don't rely on the admin filling it).** If you already compute live count tokens (`{articles}/{issues}/{volumes}/{years}` via the issue/submission Collectors' `getCount()`), use them as a FALLBACK: when the stats feature is enabled but the admin entered no stat pairs, build a default set from those tokens (filtering out zero metrics so a new journal isn't a row of zeros) — gated to the homepage template + run once. Pair with a per-card count-up: observe each `.stat` (not the row) with an IntersectionObserver and animate `0 → N` on entry, plus a staggered `transition-delay` per card. Use **count metrics** for the animated numbers, never a literal year like "Since 2015" (count-up `0→2015` looks broken). Remember to gate the row's load-fade off under the JS `.alnakib-anim` class so the scroll-reveal owns the cards.
- **"Make everything editable/toggleable" — two cheap mechanisms.** (1) **Spacing as injected CSS vars with a fallback**: add `FieldText` options (blank default) for header padding, footer top-margin, footer padding, content↔sidebar gap; inject `--var:<value>;` into `:root{}` ONLY when non-empty (so the shipped default is byte-identical) and consume it in the rule as `var(--footer-mt,60px)`. Sanitize the value (`str_replace([';','{','}'],'',$v)`) so it can't break out of the declaration — these are manager-trusted like Custom CSS, but it's cheap defense-in-depth. (2) **Per-block toggles**: every sidebar block should have its own `FieldOptions` show/hide (search box, current-issue widget, keywords, info links, recent articles, How-to-Cite…), gated in the partial as `{if $alnakibShowX}`. When wrapping an existing un-toggled block in `{if}`, you can add the closing `{/if}` by editing the NEXT block's opening line (e.g. prepend `{/if}\n\n` to `{if $nextBlock}`) — keeps the Smarty tag count balanced in two surgical single-line edits. The "How to Cite" content is already built server-side (`$alnakibCitation`/BibTeX/RIS), so a sidebar How-to-Cite widget is just a second render of it behind its own toggle.
- **OJS 3.5 reader-theme research map (verified against `stable-3_5_0`).** Hard-won reference for "what else can a premium theme surface". **(A) The COMPLETE set of frontend `{call_hook}`s is only ~10** (don't expect more from old docs): `Templates::Article::Main` (article body, after abstract, before usage-stats canvas), `Templates::Article::Details` (entry-details sidebar — this is where the **citationStyleLanguage** plugin injects its 11-style "How to cite"), `Templates::Article::Details::Reference` (per parsed citation), `Templates::Article::Footer::PageFooter` (below article — where **recommendByAuthor**/**recommendBySimilarity** render `articlesBySameAuthor`), `Templates::Issue::Issue::Article` (each TOC summary — decorate every entry: OA pill, galley-count), `Templates::Index::journal` (homepage), `Templates::Search::SearchResults::PreResults` + `::AdditionalFilters`, `Templates::Common::Sidebar` (ALL BlockPlugins render here), `Templates::Common::Footer::PageFooter`. There is **no** `Templates::Common::Header::Head` hook — `<head>` injection is `TemplateManager->addHeader($name,$html,['contexts'=>[...],'priority'=>N])` rendered by `{load_header}`. Preserve these hooks in overridden templates so bundled plugins keep working. **(B) Controlled-vocab metadata the default theme DROPS** (only renders keywords): `$publication->getLocalizedData('subjects'|'disciplines'|'supportingAgencies'|'coverage')` — in 3.5 each item is a `{name}` object (same shape as keywords, accessed `$kw.name`), so normalise defensively (string|`['name']`|object) and render as chips; `supportingAgencies` is the de-facto "Funding" line (3.5 has **no** core FundRef funder/award schema). Also unrendered & cheap: `getLocalizedData('type'|'rights')`, `getData('source')`. **(C) Per-author (core `author.json`)**: `getData('competingInterests')` (multilingual disclosure) and `getData('creditRoles')` (CRediT — array of role URIs; needs the **credit** plugin enabled, off by default; map URI slug→name yourself). `dateAccepted` does NOT exist (only `dateSubmitted`/`datePublished`). **(D) Issue**: `getData('accessStatus')` + `getData('openAccessDate')` for an OA/embargo badge. **(E) `addOption` field types that exist in 3.5** (resolve `PKP\components\forms\<Type>`): FieldUploadImage, FieldSlider, FieldRichTextarea, FieldColor, FieldHTML, FieldPreparedContent, FieldControlledVocab — **no FieldArchive**. **(F) Highlights** (3.4+ homepage feature) ship as a **Swiper carousel** (`highlights.tpl`); a theme that drops the default JS MUST override `highlights.tpl` with a static zero-JS card grid or it renders broken. **(G) Don't-add list**: Google **retired the `WebSite`+`SearchAction` sitelinks searchbox (Nov 2024)** — remove if present; never emit `citation_*`/`DC.*` from the theme (bundled googleScholar/dublinCoreMeta own them; core `headerHead.tpl` is minimal so the theme's canonical/JSON-LD/OG/hreflang are non-duplicative). **(H) WCAG 2.2 AA new criteria** worth a pass: 2.4.11 Focus-Not-Obscured (sticky-nav vs focused anchors — `scroll-padding`), 2.5.8 Target-Size ≥24×24 for standalone icon controls (inline-in-text icon links are exempt).
- **Integrating the CitationStyleLanguage (CSL) plugin's "How to Cite" — placement + styling (verified on a live 3.5 site).** When a journal enables CSL (very common), it renders a multi-format "How to Cite" (`<div class="item citation">` → `.citation_display .label`, `#citationOutput .csl-bib-body .csl-entry`, a "More Citation Formats" `.citation_formats_button` toggling `.citation_formats_list[aria-hidden]`, two `.citation_formats_styles` lists for the 11 styles + RIS/BibTeX downloads with `.fa.fa-download`) via the **`Templates::Article::Details`** hook — and the **default theme fires that hook in the SIDEBAR**. So: (1) if your theme placed `{call_hook name="Templates::Article::Details"}` in the main `<article>`, the citation lands in the main column — **move the hook into the `<aside>`** to match default (and to honour "put it in the sidebar"). (2) **The CSL plugin's stylesheet loads AFTER theme.css**, so to restyle its output your rules must out-specify it — prefix every selector with a wrapping class like `.page_article` (raises specificity one class above CSL's class-only selectors, winning regardless of source order). Style `.item.citation` as a card, the format button as a btn, the `.citation_formats_styles a` as pill chips, and respect CSL's JS by hiding `.citation_formats_list[aria-hidden="true"]`. Keep your own hand-built citation as the FALLBACK (gated on its data) for journals with CSL off — they won't both show because the hand-built one is empty when CSL owns citations. (3) **recommendBySimilarity / recommendByAuthor** render via `Templates::Article::Footer::PageFooter` as an UNSTYLED `#articlesBySimilarityList` / `#articlesBySameAuthorList` (`> .label` heading, `> ul > li` rows with a title link + an issue link, a `.cmp_pagination`, a similarity-search `<p>`) — style them as a card grid (first `<a>` = title prominent, last `<a>` = issue muted), again `.page_article`-prefixed and dark-mode/RTL safe.
- **Link author names to the frontend author search** — readers expect "click an author → their other work". Use the SAME idiom as keyword links: `{url page="search" op="search" authors=$name}` (the `authors` GET param maps to the advanced-search form's author field; `query=` is the all-fields equivalent used for keywords — confirm the field names from your own reskinned `search.tpl`). Wrap ONLY the visible name in the `<a>`, not the affiliation superscript or the ORCID icon, and do it on the **per-author academic list** — the TOC/summary uses a single concatenated `getAuthorString()` that can't be split per-author without rebuilding it. Keep the link **mode-safe**: `color:inherit` + underline-only on hover — a `color:var(--primary)` hover goes dark-on-dark in dark mode (since `--primary` is the dark navy). Add a localized `title` ("More articles by this author") as a sighted hint; the visible name stays the accessible name. Caveat for bilingual sites: it searches by the displayed name string (as fuzzy as any name search) — fine, but note OJS has no canonical per-author page.
- **Don't assume a bundled plugin's URL — many moved OUT of core in 3.4+.** The Web Feed (RSS/Atom) plugin is no longer in the `pkp/ojs` repo at `stable-3_5_0` (it became a separate Plugin Gallery repo, installed on demand), so a theme cannot hardcode its gateway feed URL or assume it's even installed. To surface feeds, detect the plugin at runtime via `PluginRegistry` and build the URL from its own API — or skip it rather than ship a link that 404s. Same caution for any non-core plugin (Usage Stats / ORCID / DOI are in core; many others are gallery installs): verify presence before linking to their handlers.
- **Preload the LCP image — but ONLY the one the template actually eager-loads.** A `<link rel="preload" as="image" href="…" fetchpriority="high">` emitted into `<head>` (via `addHeader` in the `TemplateManager::display` hook) starts the largest-contentful image fetch before the parser reaches its `<img>`, measurably improving LCP for an image that sits below the masthead/nav/announcements (e.g. a homepage hero cover). The trap: **preloading an image the page lazy-loads or never renders is wasted bandwidth (and can hurt)**, so replicate the template's eager-vs-lazy condition exactly in PHP. Concretely, a homepage hero cover is the LCP only when there's a current-issue cover AND no highlights above it (highlights push it below the fold → it's lazy), so gate the preload on the cover URL being non-empty AND `getTemplateVars('highlights')` being empty (it's a Collection — guard with `$hl && method_exists($hl,'count') ? $hl->count() : 0`), and only on the index template. Keep the matching `fetchpriority="high"` on the `<img>`; preload + fetchpriority together is the complete LCP treatment.
- **Show an article's language with the PHP-intl endonym, not a hand-rolled code→name map.** A bilingual/multilingual journal benefits from a "Language" line on the article. The publication carries a `locale` field (`$publication->getData('locale')`); turn it into a human name with `\Locale::getDisplayLanguage($loc, $loc)` — passing the locale as BOTH the code and the display-locale yields the **endonym** ("العربية", "English", "français"), which is self-describing and needs no current-UI-locale lookup. Guard it (`class_exists('Locale')` — intl is required by OJS but be defensive — plus `try/catch`), and **never `ucfirst()` the result**: `ucfirst` is byte-based and corrupts a multibyte Arabic first character. Assign it only inside the article-page branch of the hook and gate the template item on the value being non-empty.
- **Making a feature-rich theme's settings usable: OJS has NO native grouping for theme options, AND a child theme inherits its parent's options.** Two compounding problems turn a big theme's settings form into an endless scroll. (1) `PKPThemeForm` shoves EVERY theme option into a single `default` group (it force-sets `groupId='default'` on each field), so `addPage()`/`addGroup()`/a per-option `groupId` do nothing — there is no native tab/section support for theme options. (2) `setParent('defaultthemeplugin')` runs the parent's `init()` **synchronously** and `getOptionsConfig()` merges the parent's options in (parent-walking), so the child's form ALSO shows the Default theme's `typography` / `baseColour` / `showDescriptionInJournalIndex` / `useHomepageImageAsHeader` / `displayStats` — duplicating any font/colour/homepage controls the child reimplements. Fixes: **(a) Strip the inherited duplicates** — `removeOption($name)` recurses into the parent (`return $this->parent ? $this->parent->removeOption($name) : false`), and because `setParent` already ran the parent's init, the parent options exist and are removable immediately after `setParent()`: `foreach ([...] as $o) { $this->removeOption($o); }`. **(b) Add client-side tabs** — a theme can load admin assets with `addStyle/addScript($name, $path, ['contexts' => 'backend'])` (same `'contexts'` key as `'frontend'`/`'htmlGalley'`). Mark your section dividers with a class (a `FieldHTML` option whose description is `<div class="my-sec">…</div>`), then a small **backend** JS turns the flat `.pkpFormField` list into tabs: tag each field with the index of the nearest preceding marker (`data-…-sec`), render a sticky tab bar from the marker titles, and reveal one section via CSS — `body[data-tab="N"] .myform .pkpFormField[data-sec]:not([data-sec="N"]){display:none!important}`, one rule per index, with the marker rows hidden (`.my-sec-row{display:none}`). Make it **bullet-proof** because you're enhancing a Vue form you can't easily test: **no-op if the markers aren't found** (inert on every other admin page; degrades to the plain long form); drive visibility from a `<body>` attribute (a node Vue doesn't manage) plus per-field data-attrs re-applied by a **debounced MutationObserver that `disconnect()`s while it mutates** (avoids self-trigger loops; survives the form's async mount and settings-sub-tab remounts); **never touch field names/values** (so the form still submits); and include an **"All" tab** escape hatch so a mis-tag can never hide a setting permanently. This is the only way to deliver "tabbed theme settings" in OJS 3.5 today. Two follow-ups: (a) the per-section CSS hide rules are index-based (`body[data-tab="N"] .form .pkpFormField[data-sec]:not([data-sec="N"]){display:none!important}`), so author them with HEADROOM beyond your current section count (e.g. indices 0–23 for ~14 sections) — past the cap an extra section's fields show on every tab. (b) **PHP audit fact worth remembering: passing too MANY positional arguments to a PHP function/method does NOT throw** — only too FEW raises `ArgumentCountError`; extra positional args are silently ignored. So a stale 2-arg call like `Repo::issue()->getCurrent($id, true)` against 3.5's 1-arg signature is HARMLESS (the `true` is ignored), not a fatal — an audit that calls it a CRITICAL ArgumentCountError is wrong. Normalise it to the correct arity anyway (stops it re-flagging, and is safe under any signature), but don't treat it as a page-breaking bug.
- **High-value article-sidebar blocks (live data, no plugins needed).** Beyond downloads/issue/categories/versions/license, two clean additions: (a) a **"Publication dates"** key/value list — `Received` = `$article->getData('dateSubmitted')` (it's on the *Submission*, not the Publication) and `Published` = `$publication->getData('datePublished')`, rendered as a `<dl>` of label/value rows (date metadata journals are expected to show, and it's not in the meta bar). (b) an **"Article tools"** block of compact actions: a **Cite** link that jumps to the How-to-Cite section (give that `<section>` an `id` and rely on the `:target{scroll-margin-top}` rule so it clears the sticky nav), **BibTeX/RIS** download links via `data:` URIs built from the already-assembled citation strings (`href="data:application/x-bibtex;charset=utf-8,{$bib|escape:'url'}" download="citation.bib"`), and a **Print** `<button class="js-print">` wired in JS to `window.print()` — which leverages the theme's existing `@media print` stylesheet for free, so a reader gets a clean printout with one click. Gate the cite/BibTeX/RIS on the same `showCite` option that assigns them; print is always available. Do NOT add an article-metrics block here — view/download counts come from the UsageStats plugin (which injects into `Templates::Article::Main`); a theme-built one double-queries and double-displays. Style the date `<dl>` and tool buttons from palette vars so they inherit dark mode + RTL automatically.
- **A premium "On this page" scroll-spy reading-nav (article sidebar) — three correctness traps.** A pinned section navigator that highlights the part of the article currently in view is the single highest-impact "premium feel" add for the abstract page, and it needs no plugin or extra query — the sections already exist. **(1) Server-render the list, gate every `<li>` on the EXACT condition that renders its section**, so the TOC can never list a section that didn't appear (and works with JS off). The simple sections re-check their own getter (`{if $publication->getLocalizedData('abstract')}…{/if}`, references on `$parsedCitations|@count || …citationsRaw`, bios on the `$hasBios` counter the main flow already computes); the one that's expensive to re-derive — the **inline-PDF** block, whose condition includes a galley-type loop — should **set a flag as it renders** (`{assign var=hasInlinePdf value=true}` initialised `false` near the top) that the sidebar then reads, since the sidebar Smarty executes *after* the `<article>`. Add stable `id`s to each section (`#abstract`,`#references`,`#author-biographies`,`#full-text`; reuse the ones data-availability/how-to-cite already have) and **gate the whole widget on 2+ sections present** (count them first) so a short note doesn't get a one-item stub. **(2) `position:sticky` needs a tall containing block** — the article grid is `align-items:start`, so the sidebar cell is only as tall as its own content and a sticky nav would scroll away after a screenful. Override just that cell: `.art-detail-grid > .sidebar{align-self:stretch}` so the aside fills the article's height and the pinned nav travels the full read (the aside is transparent, so the extra height is invisible). **(3) The scroll-spy itself**: an `IntersectionObserver` with a **narrow active band** (`rootMargin:'-20% 0px -70% 0px'`) marks a section "in view" only while it crosses the top quarter; track a visible[]-by-index set and pick the **topmost** visible link, with a fallback that highlights the **last section whose top is above the line** when you're between sections (otherwise the highlight blanks out in gaps). Toggle `.is-active` **and** `aria-current="true"` together. Wire click to `scrollIntoView({behavior:'smooth',block:'start'})` (respects the section's `scroll-margin-top` so it clears the sticky site-nav) **only when not `prefers-reduced-motion`** — else let the native `#hash` jump happen — and move focus to the target (`tabindex=-1` + `focus({preventScroll:true})`) for keyboard/SR users, updating the hash via `history.replaceState`. **Hide it on mobile** (`@media(max-width:859px){.reading-nav{display:none}}`): once the layout stacks, the sidebar sits *after* the article, so a "jump to section" list there is pointless and a sticky element would fight the footer. Ship it behind an admin toggle defaulted ON (it's a pure reading aid), localised heading ("On this page" / «في هذه الصفحة»).
- **A sidebar that's consistent on EVERY page (not just home/article).** Out of the box an OJS theme only shows a sidebar where a block plugin fills the `Templates::Common::Sidebar` hook AND the page isn't `isFullWidth` — so inner pages (issue, archive, search, about/contact/CMS) typically have **no** sidebar while bespoke pages do, which reads as "the sidebar isn't unified." Fix it in ONE place: in `footer.tpl`, render a shared **`journalSidebar.tpl`** partial inside the `.pkp_structure_sidebar` `<aside>` for all non-`isFullWidth` pages (block-plugin output, if any, below it). The theme's `.pkp_structure_content:has(> .pkp_structure_sidebar){grid-template-columns:1fr 312px}` then turns every such page two-column automatically — no per-page edits. Three rules to get it right: (1) the shared partial must use **only globally-assigned vars** (search, submit CTA, info links, keywords, custom widgets — assigned on every frontend page) so it's page-agnostic; do NOT reference page-specific objects like the viewed `$issue`. (2) Pages that render their OWN sidebar (homepage, article) must set `{assign var="isFullWidth" value=true}` so the footer slot is suppressed and you don't get a **double** sidebar. (3) Set the **multi-journal site index** (`indexSite.tpl`) to `isFullWidth` too — a single-journal sidebar has no `$currentContext` there. To let that shared partial show a **"Current Issue"** widget on EVERY page (not just the homepage, which alone gets `$issue`), assign the current issue globally from the hook — `Repo::issue()->getCurrent($contextId, true)` (cached) plus a global `$alnakibPublisher` for the generated-cover label — and have the partial render the widget from those globals; gate it on the object existing so off-context pages skip it. Keep the homepage's own sidebar and the shared partial in sync — or, better, have the homepage `{include}` the partial to avoid the ~75-line duplication that otherwise drifts. **Header note:** offer a `mastheadShowTitle` toggle to hide the text wordmark when the uploaded logo already contains the journal name (otherwise the name shows twice — logo + wordmark); the logo link's `aria-label` keeps it accessible. And never fall the masthead subtitle back to `searchDescription` — that's typically the name again, duplicating the title. Once the sidebar is everywhere, make its **width a single admin control**: route every sidebar grid (homepage `.layout`, article `.art-detail-grid`, inner `.pkp_structure_content:has(> .pkp_structure_sidebar)`) through one `var(--sidebar-w,312px)` and inject `--sidebar-w` from a `sidebarWidth` option — don't leave each grid on its own hardcoded px. Likewise a **position** option (left/right) should flip **all** of those grids together (swap the column template to `var(--sidebar-w) 1fr` and set `order` on the two children of each grid), not just the homepage one, or "left sidebar" looks inconsistent across pages. Same single-control discipline applies to an optional hero: a `heroStyle` radio (`soft` light band vs `brand` deep-navy band) is just a class toggle (`.home-hero.hero-brand{background:linear-gradient(...navy...);}` + light text) — cheap to offer, and it lets the one hero serve very different journal brands.
- **"My new template feature doesn't show after uploading the theme" is almost always the compiled-template cache.** OJS compiles Smarty to `cache/t_compile/`; after uploading a new theme version, a newly-added block/partial may not appear until you **Clear Template Cache** (Admin tools) / delete `cache/t_compile/*`. Before debugging template logic, confirm (a) the right theme is the **active** one for that journal (the page can silently fall back to Classic/Default), and (b) the cache is cleared. Relatedly, treat **citations as always-on**: build the APA/BibTeX/RIS strings unconditionally on the article page and gate display on the citation data existing, not on an admin toggle — a core scholarly affordance shouldn't be hideable by an accidental setting.
- **Dark mode that doesn't flash:** if your design system is built on CSS custom properties, dark mode is mostly a `:root[data-theme="dark"]{ … }` block that **remaps the neutral/paper/ink variables** (keep navy/brass scales — masthead/nav/footer are already dark) plus ~15 overrides for the spots that hard-code `#fff` or `navy-900` text. Set `data-theme` **in `<head>` before first paint** via a tiny inline `addHeader` script (`localStorage` choice → else `matchMedia('(prefers-color-scheme: dark)')`), or you get a white flash; a deferred footer script only handles the manual toggle's click + persistence. Make it an **admin choice** — a `FieldOptions` radio (`light` = no dark mode / `toggle` = visitor switch + remembers / `auto` = follow device, no button); the head script reads `localStorage` only in `toggle` mode and is omitted entirely for `light`. After building dark mode, **grep your CSS for every `color:var(--navy-900|800)` and `background:#fff`** — the variable remap misses any rule that hard-codes a dark text colour or white surface (titles, `.prose h3`, form labels/legends, `.mb-v`, contact/masthead-list names), so each needs an explicit `[data-theme="dark"] …{color:var(--ink)}` / `background:var(--card)` override or it goes dark-on-dark. **One thing the variable remap can't reach is native UI** — scrollbars, the page canvas behind your backgrounds, and the default look of `<input>`/`<select>`/checkbox/radio on OJS's register/contact/advanced-search forms stay light-rendered, so they clash on the dark canvas. Fix it with the `color-scheme` property, not more variables: `:root{color-scheme:light}` and `:root[data-theme="dark"]{color-scheme:dark}` — the browser then paints native widgets, the canvas, and the OS scrollbar in the matching scheme. (Bonus: it also fixes the inverse bug — an OS-dark visitor viewing your *light-only* theme would otherwise get dark native controls.) Pair it with `accent-color:var(--navy-600)` (and a mode-appropriate value in the dark block, e.g. `var(--brass-500)`) to brand-tint checkboxes/radios/range/progress in one line, and, if you want a unified scrollbar, palette-driven pseudo-elements that follow the vars automatically: `html{scrollbar-color:var(--line) transparent}` (Firefox) + `::-webkit-scrollbar-thumb{background:var(--paper-3);border:3px solid var(--bg);border-radius:8px}` with a `:hover` brass — because they reference the remapped vars, they recolour for free in dark mode.
- **Context-aware chrome on the multi-journal site index.** `$currentContext` is null there, so journal-specific masthead bits — the "Make a Submission" CTA (submit to *which* journal?) and a journal-frequency meta line — should be gated with `{if … && $currentContext}`. The site index also falls back to `$siteTitle` for the name and needs the site-locale switcher. Keyboard-accessible dropdowns are free with `li:focus-within > ul{display:block}` (no JS). Privacy-friendly article sharing = plain intent links (`twitter.com/intent/tweet`, `linkedin.com/sharing/...`, `mailto:`) built from `{url}` + `|escape:"url"` — no third-party scripts; pair with a `navigator.clipboard` copy-link button.
- **Use inline SVG for UI icons, never Unicode dingbats.** Glyphs like ☀ (U+2600), ☾ (U+263E) and ⌕ (U+2315) render as **tofu/`?`** in serif/text fonts (Spectral, Amiri, etc. don't include them) — a real "the icon is broken" bug. Ship tiny inline `<svg fill="none" stroke="currentColor">` icons (sun/moon for the dark toggle, magnifier for search, chevron for back-to-top) so they inherit colour and always render. Basic typographic arrows (`→` U+2192, `↑` U+2191) technically render in text fonts, BUT treat even decorative link arrows as icons — many clients want a strictly **no-emoji / no-glyph-icon** package, so render "more →" / "← back" affordances as small inline SVG **chevrons** (`<path d="m9 6 6 6-6 6"/>` right, `m15 6-6 6 6 6` left), wrapped in your RTL-flip span so they mirror in Arabic. Keep genuine punctuation (·, —, ©, « ») — that's typography, not an icon. In help/description text use a plain `>` for nav paths ("Settings > Website"), not an arrow glyph. (Authoring note: write such templates with a real editor — non-ASCII glyphs piped through a PowerShell here-string get mangled. Audit a finished theme for stray glyphs with a unicode-range grep over the 2190–27BF / 1F000–1FAFF blocks, and confirm no `claude`/`anthropic`/`agent`/AI strings or `Co-Authored-By` trailers ship in the package.)
- **Ship an article print stylesheet (`@media print`).** Researchers print the HTML article page, not just the PDF. Hide the OJS/theme chrome by class — `.util-bar,.masthead,.nav,.pkp_structure_footer_wrapper,.index-strip,.sidebar,.pkp_structure_sidebar,.cmp_breadcrumbs,.cmp_skip_to_content` plus your own interactive bits (`.art-share,.theme-toggle,.nav-toggle,.to-top,.reading-progress,.obj_galley_link,.download-box`) — then flatten the article's `display:grid` to `display:block` so the two-column `.art-detail-grid` becomes one readable column. Force black-on-white, de-pill keyword/galley chips (`background:none;border:1px solid #999`), `page-break-inside:avoid` on `.references li`/`.abstract-block`, and expand link targets with `a[href^="http"]::after{content:" (" attr(href) ")"}` so citations survive on paper. Note the bespoke article sidebar is `<aside class="sidebar">`, so hiding `.sidebar` already drops its cover/download/issue widgets — don't add redundant per-element hides.
- **Mobile checklist for a custom OJS theme.** Collapsing the obvious grids (`.layout`, `art-detail-grid`, footer, archive) isn't enough — the masthead and utility bar are the usual phone-breakers. (a) **Drive horizontal padding through one CSS var** (`.wrap{padding-inline:var(--gutter)}`, nav uses `calc(var(--gutter) - 14px)`) and **shrink it in media queries** (`@media(max-width:640px){:root{--gutter:20px}}`, `400px→14px`) — one line re-flows every full-width strip and you reclaim ~50px on a 360px screen. (b) **Stack the masthead**: `flex-wrap:wrap` the brand row, shrink the logo (72→54→46px) and title, `display:none` the brand-divider + masthead-spacer, and make the "Make a Submission" CTA a **full-width row** (`.masthead-cta{width:100%}` + `.btn{width:100%}`). (c) **Utility bar**: `flex-wrap:wrap` it and, below ~520px, `display:none` the ISSN (`.util-left`) since it repeats in the footer — otherwise login/register/lang/dark-toggle overflow. (d) **Stop sideways scroll from content you don't control**: `@media` → `.prose table,.abstract-block table{display:block;max-width:100%;overflow-x:auto}` and `overflow-wrap:anywhere` on long DOI/identifier/keyword strings (`.mb-v`,`.issue-doi`,`.kw`). (e) The **htmlGalley** document is isolated, so it needs its **own** mobile rules (tighter `body` padding + scrollable tables) in `htmlGalley.css`. (f) `repeat(auto-fill,minmax(300px,1fr))` grids (journal cards) self-collapse — no breakpoint needed. (g) When a two-column layout (article body + details sidebar) collapses, the sidebar becomes **full width** — so any `img{width:100%}` in it (the article cover) balloons to the whole screen; cap it (`max-width:260px;margin-inline:auto`) at the same breakpoint where the grid stacks.
- **Make `error.tpl` a recovery point, not a dead end.** The 404/error page is one users genuinely hit — add a search box + quick links (Home, Current issue, Archives) below the message, but **gate the journal-specific ones (`search`, `issue/current`, `issue/archive`) on `$currentContext`**: a *site-level* error has no journal, so those `{url}`s would be context-less/invalid — there, leave only `{url page="index"}` (the journal list). Keep the core `$backLink`/`$errorMsg`/`$errorParams` vars.
- **Respect real uploaded covers *everywhere*, not just the archive.** If the theme draws a stylised CSS "generated cover" (acronym + volume on a gradient) when no artwork exists, gate it on `{assign var=cover value=$issue->getLocalizedCoverImageUrl()}{if $cover}<img …>{else}…generated…{/if}` in **every** place a cover renders — homepage current-issue banner, homepage sidebar cover widget, archive card (`issue_summary.tpl`) **and** the issue landing (`issue_toc.tpl` with `hideCover=false`). It's easy to add the fallback to the archive card and forget the homepage, so a journal that uploads covers sees them on `/issue/archive` but a generated mark on the front page. For the real-image variant, reuse the `.cover` frame but zero its padding and fill it: `.cover.cover-real{padding:0}.cover.cover-real img{width:100%;height:100%;object-fit:cover;display:block}` (keep the `aspect-ratio:3/4` and the brass spine `::before` so both variants share one silhouette). Always pass `alt=$issue->getLocalizedCoverImageAltText()|default:$issue->getIssueIdentification()|strip_tags`.
- **Mobile nav: the drawer the hamburger toggles is often silently dead because a LATER base rule wins on source order.** A subtle, easy-to-miss bug class in a single big theme.css: media queries add NO specificity, so a `@media(max-width:768px){.nav .wrap > ul{display:none}}` rule placed EARLY in the file is overridden by an equal-specificity base rule that appears later (the OJS-bridge `.nav .wrap > ul{display:flex}` / `ul ul{display:none;position:absolute}` that styles the `{load_menu}` output) -- so on a phone the menu stays expanded and the toggle no-ops, while dropdown submenus stay `position:absolute` + hover-only. Fix: put the authoritative responsive overrides AFTER the base rules they override (append the mobile-nav block near EOF), or raise specificity (e.g. `.nav.nav-open .wrap > ul`). When in one file, "later wins" is the lever -- not !important. (b) **Hover dropdowns are unreachable on touch** -- a coarse pointer can't reveal `li:hover > ul`, and tapping a parent `<a>` with its own href just navigates. So inside the OPEN drawer, expand nested `<ul>`s inline (`position:static;display:block`, indented + tinted) so every child link is tappable; collapse them again when the drawer is closed. (c) **Cap the open drawer's height and let it scroll internally** (`max-height:min(72vh,560px);overflow-y:auto;overscroll-behavior:contain`) so a long menu on a short screen doesn't push the sticky bar off-screen or chain-scroll the page. (d) **Reset a stale open state at the desktop breakpoint** -- a `matchMedia('(min-width:769px)')` change listener that calls your closeNav() keeps `aria-expanded` honest after a rotate/resize (the CSS already reverts to the horizontal bar, but the class/aria linger). (e) **Touch-target sizing without touching desktop**: WCAG 2.2 SC 2.5.8 wants ~44px tap areas; grow the cramped util-bar/lang/dark-toggle/nav-search controls only under `@media(pointer:coarse)` so mouse/desktop spacing stays tight. (f) **Horizontal-overflow safety net that doesn't break sticky**: use `overflow-x:clip` (NOT `overflow-x:hidden`) on the page wrapper -- `hidden` establishes a scroll container that can kill `position:sticky` on the nav, whereas `clip` does not (and `clip` + default `overflow-y:visible` is a legal pair that leaves both axes as-authored).
- **CRediT contributor roles + competing-interests are OJS 3.5 core author metadata that core renders on NO frontend page -- a clean theme differentiator, but verify the shape before you build.** Two verified facts from pkp source (stable-3_5_0): (a) `competingInterests` IS in `pkp-lib/schemas/author.json` as a multilingual nullable string ("A declaration of potential competing interests") -- safe to read with `$author->getLocalizedData('competingInterests')`. (b) CRediT moved into core in 3.5 (roles AND a degree of `lead`/`equal`/`supporting` per role), and PKP's own 3.5 notes say it "should be displayed on the article landing page" -- yet `pkp/ojs` `templates/frontend/objects/article_details.tpl` renders NEITHER, and there is NO `getCreditRoles()` accessor on the `Author` class, so it's reached generically via `$author->getData('creditRoles')`. The catch: the stored SHAPE is not obvious from the schema (it can be a list of role-URI strings, a list of `['role'=>uri,'degree'=>...]` maps, or an associative `uri=>degree` map depending on version/path). **So normalise it in PHP, never in Smarty:** a helper that accepts all three shapes, extracts a slug from each CRediT URI (`#contributor-roles/([a-z-]+)#`), maps it to a label via a curated 14-role table with a slug-humanise fallback, and DROPS anything unmappable -- so the template can never print a raw URI -- then returns a clean `[['name'=>..,'roles'=>[['label'=>..,'degree'=>..]]]]` the template just loops. Gate display on an admin toggle AND on the data being present, and **never fabricate a "the authors declare no competing interests" line** -- only render what an author actually declared. Localise just the section headings ("Contributor roles" / "Competing interests"); CRediT role names are a standardised English taxonomy, so showing them in English even on an RTL/Arabic page is conventional (degree = lead/equal/supporting). Render roles as chips (degree as a small mono sub-label) so no separator glyph is needed. (Method-verification tip: a 2-minute WebFetch of the pkp raw `schemas/author.json` + the app's `article_details.tpl` tells you exactly what core stores and renders -- cheaper than guessing and shipping a block that prints URIs.)
- **Site-wide entrance motion + a calibrated design audit: lessons from a full "make every page feel professional" pass.** (a) **Promote the homepage scroll-reveal to ALL frontend pages.** A common pattern is an IntersectionObserver that adds `.in` to `.reveal` elements, gated by a `.alnakib-anim` class the JS sets on `<html>` ONLY after the reduced-motion check (so JS-off / reduced-motion users never get hidden content). The mistake is scoping it to `document.body.classList.contains('pkp_page_index')` — drop that and it animates issue/archive/search/about/contact/masthead/announcement pages too, which is what makes the whole site feel cohesive. Two correctness traps when you generalise it: (i) **a fixed `transition-delay` stagger reads as LAG for scroll-triggered reveals** — an item 40 cards down with a 400ms delay visibly waits before fading after you scroll to it. Fix: only apply the stagger delay to items whose `getBoundingClientRect().top < innerHeight * 1.3` at load (the first screen); everything below reveals instantly on scroll. (ii) **double-animation** — if the same element ALSO has a CSS load-time `@keyframes` (e.g. `.sidebar .widget{animation:fade-up}`), it fires alongside the JS reveal. Neutralise it with a guard rule `.alnakib-anim .sidebar .widget,.alnakib-anim .toc-section .article-item{animation:none}` so the JS reveal cleanly owns those elements. (b) **Add motion/shadow/rhythm TOKENS, consume with var() + fallback:** `--ease:cubic-bezier(.22,.61,.36,1)`, a 3-step `--t-fast/--t/--t-slow` duration scale, `--shadow-lg`/`--shadow-brass`, `--section-gap` — then `transition:transform var(--t,.2s)` everywhere so timing is coherent and tweakable in one place (don't forget the dark-mode `--shadow-*` overrides). (c) **A reusable `.empty-state` card** (centred card + inline-SVG icon + serif title + muted hint + optional action button) is the clean fix for OJS's bare `<p>{translate ...noItems}</p>` branches — wire it into no-current-issue, no-search-results, and no-category-items; it needs ~2 new localised hint strings. (d) **Restricted/paywalled galley affordance:** a dashed brass outline (`border:1px dashed var(--brass-500)`) reads as "gated" far better than the near-invisible `opacity:.92` dim core ships — and needs no lock glyph. (e) **DON'T blindly trust an automated "WCAG contrast failure" finding — compute it.** An audit agent flagged the utility bar (`navy-100` #d6e2ee on `navy-950` #06152b) and `.jmeta` (`navy-300` on `navy-900`) as AA failures; actual ratios are ~14:1 and ~6:1 — both PASS. "Fixing" them would have damaged the brand for no reason. Verify a contrast claim with the real hex values before changing palette tokens; and treat audit findings as a checklist to triage (some will be stale — e.g. flagging missing author-search links that already exist), not a to-do list to implement wholesale.
- **Three upgrades to a feature-rich theme: richer settings tabs, more live homepage data, and presets that are full design themes (not just palettes).** (a) **Settings tabs beyond hide/show:** the client-side tab bar (built from the injected `.alnakib-set-sec` markers) gains real value with (i) ARIA tablist semantics — `role=tab`/`tablist`, `aria-selected`, ROVING tabindex (active=0, rest=-1) + keyboard nav (Arrow Left/Right with RTL swap, Home/End), manual activation (Enter/Space); (ii) a **live filter box** that type-ahead-searches EVERY setting by its label across all sections at once — implement by tagging each `.pkpFormField` with `data-alnakib-sec`, then on input set `body[data-alnakib-themetab="filter"]` (a non-numeric value so none of the per-index hide rules match → all sections visible) and add `.alnakib-hit` to matching rows, with CSS `body.alnakib-filtering .pkpFormField[data-alnakib-sec]:not(.alnakib-hit){display:none}`; clearing the box restores the remembered tab; (iii) per-tab field-count badges; (iv) `scrollIntoView` on tab switch. Persist the filter value in a module var so the debounced MutationObserver (which rebuilds the bar on Vue remounts) can re-apply it. (b) **More live homepage data:** a "Latest articles" strip (most-recently-published journal-wide, distinct from the current-issue TOC) — generalise the existing `recentArticles()` helper to take a `$limit` and add a concise author string (`getFullName()`, first 3 + "et al."), gate the query on the homepage template + an admin toggle so it never runs elsewhere; and add live stat tokens like `{latestyear}` (cheap — it is `max(array_keys($yearSet))`, already computed for `{firstyear}`). (c) **Presets that change MORE than colour:** the preset map already carried `primary/accent/paper/corner/masthead/hero`; extend each entry with optional `headingFont`/`bodyFont`/`fontsize`/`wrap` keys, and in the appearance injector read them with `$preset['headingFont'] ?? trim($this->getOption('headingFont'))` (the `??` null-coalesce is safe even when `$preset` is null — it suppresses the offset-on-null warning, which is exactly why the existing colour lines work). Then ship presets that swap typography + density + width, not just hue: a **modern-sans** theme (heading+body = a `system-ui,...,sans-serif` stack so NO extra webfont loads — Arabic still uses `--arabic`/Amiri), an **editorial** mix (serif headlines + sans body), a **compact** density (smaller `--fontsize` + narrower `--wrap`), etc. A preset overrides the individual options it specifies and falls through for the rest — so "Custom" still hand-tunes everything. Remember: a preset that sets fonts via `--serif`/`--body` works instantly only if the chosen stack is system fonts OR already loaded; don't reference an unloaded webfont in a preset.
- **Mobile nav "always open", toggleable journal-content homepage blocks, usage-stat totals, and de-duplicating a plugin vs theme "How to Cite".** (a) **"Mobile menu is always expanded" has TWO classic causes, and "the CSS is correct" doesn't rule them out.** First rule out a missing viewport meta (`<meta name=viewport>`) — if the theme doesn't override `headerHead.tpl` it inherits core's, which DOES include it, so that's usually fine. The real trap: responsive CSS that works when you RESIZE a desktop window but fails on a real phone is almost always **stale CSS** — OJS serves a theme's `theme.css` at a URL with NO version query, so browsers cache it across plugin upgrades; the user sees the pre-fix file until a hard refresh + Clear Template Cache. Make the collapse itself **bulletproof** so it can't be lost to source-order or a parent-theme rule: scope it to the menu's id with `!important` — `@media(max-width:768px){.nav .wrap > ul#navigationPrimary{display:none!important} .nav.nav-open .wrap > ul#navigationPrimary{display:flex!important}}` (the `{load_menu id="navigationPrimary"}` puts that id on the `<ul>`). (b) **Let admins surface the journal's OWN managed content on the homepage by toggle** (About, Summary/description, Author guidelines, Open-access policy, Privacy) — these are all `$context->getLocalizedData('about'|'description'|'authorGuidelines'|'openAccessPolicy'|'privacyStatement')` (rich HTML). Offer them as a multi-select `FieldOptions` with `type=checkbox` (the saved value is an ARRAY — read with `in_array($key,(array)$this->getOption('homeContentBlocks'),true)`), build the list in PHP gating each on non-empty content, and render as a native `<details>`/`<summary>` accordion (zero-JS, accessible, CSS-chevron via a rotated border — no glyph). Cheap: `getLocalizedData` on the already-loaded context, no query. (c) **Total downloads / views** come from OJS's stats service: `app()->get('publicationStats')` → `getFilesTotals(['contextIds'=>[$id]])` (file downloads) and `getTotals([...])` (submission views), each returning per-submission rows with a `->metric` you `array_sum`. These are HEAVY aggregates, so compute lazily (only when a `{downloads}`/`{views}` token is actually used) and CACHE the result as a plugin setting with a TTL — `$this->getSetting($ctxId,'usageStatsCache')` / `$this->updateSetting($ctxId,'usageStatsCache',['t'=>time(),...],'object')`, refresh after ~1h — wrapped in try/catch so an unavailable stats backend yields '' and the stat card is filtered out rather than showing a label with no number. Tip: default two extra stat slots to `{downloads}`/`{views}` so journals get them out-of-the-box (unset options fall back to defaults → existing installs gain them without re-saving), and make the stat grid `repeat(auto-fit,minmax(150px,1fr))` so 4 OR 6 cards both lay out cleanly. (d) **Two "How to Cite" blocks looked generic/duplicated** because the CitationStyleLanguage plugin renders through `Templates::Article::Details` AND the theme rendered its own citation widget unconditionally. Fix by CAPTURING the hook and only falling back when it's empty: `{capture name=csl}{call_hook name="Templates::Article::Details"}{/capture}{if $smarty.capture.csl|trim}{$smarty.capture.csl nofilter}{elseif $themeCitation}…branded card…{/if}` — never both, and style the fallback as a real card (brass-edged citation + pill format chips), not a grey box.
- **Auth pages (login/register/lost-password): suppress the sidebar from PHP and restyle CORE markup you verified, not markup you guessed.** (a) The shared-sidebar slot pattern (footer.tpl renders journalSidebar inside `{if empty($isFullWidth)}`, header.tpl mirrors it for the has_sidebar grid class) gives you a one-line per-page kill switch: in the `TemplateManager::display` hook, `if (in_array($template,['frontend/pages/userLogin.tpl','frontend/pages/userRegister.tpl','frontend/pages/userRegisterComplete.tpl','frontend/pages/userLostPassword.tpl'],true)) $templateMgr->assign('isFullWidth', true);` -- auth pages become a single focused column with NO template overrides. (b) Before styling core forms, fetch the real 3.5 markup (pkp-lib `templates/frontend/pages/userLogin.tpl`, `userRegister.tpl`, `components/registrationForm.tpl` -- note they are in pkp-lib, NOT pkp/ojs): labels WRAP inputs as `label > span.label + input`; login wrappers are `.username/.password/.remember.checkbox/.buttons`; registration is `fieldset.identity` (.given_name/.family_name/.affiliation/.country) + `fieldset.login` (.email/.username/.password x2) + `fieldset.consent` (.optin rows) + `.reviewer`; the submit is `button.submit` with cross-links `a.register`/`a.login`. So "labels above fields" = style `span.label` as a block with margin-bottom (small-caps caption), and "pair short fields" = grid on `fieldset.X .fields` with full-row exceptions via `grid-column:1/-1` (e.g. pair ONLY the two `.password` cells; forcing email+username full-row keeps auto-placement from pairing unrelated fields). Checkbox rows (.remember/.optin/.reviewer labels) must UNDO the caption treatment (sentence case, body font) or consent text shouts. (c) ORCID moved into core in 3.5; the register page injects `templates/form/orcidProfile.tpl` = `button#connect-orcid-button.pkp_button > div.orcid_button_container > {$orcidIcon}` with an UNCONSTRAINED icon -- pin it (`#connect-orcid-button img,#connect-orcid-button svg{width:20px;height:20px}`) and restyle the button or it renders as a giant unstyled blob. (d) Put the auth CSS block at EOF (source-order wins over the earlier generic .cmp_form rules you are refining). (e) Stat-token hygiene: a usage-stats token that resolves to ZERO must return '' (drop the card), not "0" -- "0 Downloads" on a new journal is anti-marketing; keep raw numbers in the cache and map at return time. (f) Scriptable audit layers beat agent audits for whole bug-classes: a locale key-diff (every plugins.themes.X key used in tpl+php vs msgid lists in BOTH POs), Smarty tag balance across ALL templates, and a used-vs-assigned template-var diff are each ~20 lines of PowerShell and prove the absence of ##key## bugs, broken pages, and undefined-var blocks deterministically -- run them BEFORE spending agents, and tell the agent auditors what already passed so they hunt elsewhere.
- **A verified-findings sweep over a mature OJS theme: the bug classes a real audit surfaces (and how to confirm them cheaply).** When an agent-audit's verification layer dies, treat raw findings as a TRIAGE LIST: most are decidable by reading your own code; only externally-uncertain claims need a fetch (here: pkp-lib confirmed the lost-password page class is snake_case `page_lost_password` -- NOT pageLostPassword -- so any camelCase selector silently never matches; and `about.onlineSubmissions.submitToSection` already CONTAINS an `<a>` in its locale string, so wrapping the {translate} in another anchor nests `<a>` inside `<a>` -- render it as prose and style the string's own link as the button via a descendant selector). Other verified classes worth re-checking in ANY theme: (1) **Smarty escape-order bug**: `{$x|escape|default:$y|strip_tags}` leaves the FALLBACK unescaped (modifiers chain left-to-right across the whole expression) -- a quote in an issue title breaks out of the alt attribute; correct order is `|default:...|strip_tags|escape`. (2) **Inline-CSS option injection**: every FieldText interpolated into an inline stylesheet needs `str_replace(['<','>','{','}',';'],'',$v)` -- `</style><script>` in a font/width option is stored XSS; and admin URLs in href/src need a SCHEME allowlist (`#^(https?://|/|\#|mailto:|tel:)#i`) because |escape does not block `javascript:`. (3) **Hook-capture fallback logic**: deciding "is plugin X off?" from "is the captured hook output empty?" is WRONG when other plugins share the hook (Publication Facts Label also fills Templates::Article::Details) -- detect the plugin itself: `PluginRegistry::getPlugin('generic','citationstylelanguageplugin')` returns null for disabled lazy-load plugins, so `$p && $p->getEnabled()` is the test; always echo the captured output regardless. (4) **JS count-up**: `toLocaleString()` mid-animation rewrites digits in the BROWSER locale (Eastern-Arabic numerals on AR-locale devices) -- animate with plain String() and land on the EXACT server-rendered text. (5) **IntersectionObserver threshold>0 + very tall elements**: max ratio = viewportH/elementH, so a 0.06 threshold is unreachable for any block taller than ~16 screens (long prose) and it stays invisible -- use threshold 0. (6) **Scroll-reveal hygiene**: add `@media print{.anim .reveal{opacity:1!important}}` (printing mid-page otherwise blanks unrevealed sections) and REMOVE the reveal class + inline stagger transition-delay after the entrance finishes, or that transition owns every later hover on the element (laggy, slow lifts forever). (7) **The same source-order disease, recurring**: re-styling a region (auth forms) from an EOF block fails wherever a MID-FILE generic rule has equal-or-higher specificity (`.pkp_structure_main .cmp_form .fields .label` is 4 classes; `.page_login .cmp_form .label` is 3 -- the generic wins regardless of order) -- match the strongest competing selector's specificity in the EOF block, and remember checkbox-row text usually sits in the SAME span.label as field captions, so an uppercase-caption design needs an explicit sentence-case undo on `.remember/.optin/.reviewer` spans. (8) **Default-English stat captions on a bilingual site**: map the known default literals to locale keys at render (`['Downloads'=>'stat.downloads',...]` -> `__()`), so AR pages localise without the admin re-typing labels per locale. (9) **Logo-capable list options**: extend a "one per line" textarea to `Name | logo URL | link URL` (split-limit 3, scheme-guard the URLs, plain lines keep rendering as text) -- one parser feeds both a footer logo strip (grayscale->colour hover; in dark mode give each logo a small WHITE CHIP because service logos are drawn for light backgrounds) and a sidebar icon list.
- **The PHP/a11y/perf audit dimensions: the highest-value OJS 3.5 theme-plugin bug classes a deep verified sweep found.** (1) **NEVER `(array)`-cast `$publication->getData('authors')`** — in 3.5 it is a Laravel LazyCollection (DAO sets `getMany()->remember()`), and `(array)$collection` yields the object's internal PROPERTIES, not the authors; every author-driven feature (citation builder, JSON-LD authors, numbered affiliations, CRediT, competing interests) then degrades SILENTLY because the helpers are try/catch-guarded — no fatal, just empty output. `foreach` iterates Traversable natively, so the fix is simply `foreach ($publication->getData('authors') ?? [] as $a)`. Smarty templates were never affected ({foreach} handles Traversable), which is exactly why the breakage hid: bylines looked fine while the PHP-built data was empty. (2) **Schema-shape traps**: 3.5 keyword items are `{name,source,identifier}` ASSOC ARRAYS (an `is_object` check alone stringifies them to "Array" in JSON-LD); `coverage` is a multilingual STRING, not a vocab array — a generic vocab normaliser needs both an `is_array($item)` item-branch and an `is_string($raw)` field-branch. (3) **Core 3.5 already emits a full hreflang set incl. a locale-LESS x-default** (PKPTemplateManager::initialize) — a theme that re-emits its own set creates a CONFLICTING second x-default (theme pointed at the primary-locale URL); keep only og:locale/og:locale:alternate, which core does not emit. Core also emits the homepage meta description from searchDescription — skip yours there. (4) **Perf gates worth auditing**: helpers assigned "globally for the shared sidebar" still run on pages that SUPPRESS that sidebar (article + auth pages) — gate the queries on the same template list; two near-identical recent-articles queries (sidebar widget + homepage strip) merge into one fetch-the-max-then-slice; every collector needs an explicit ->limit() (one missing limit hydrated a whole issue to keep 5); cache FAILURES need a negative cache too (back-date the timestamp so a broken stats service retries in ~10min, not per render); memoize anything callable twice per render (localeToggle ran in addSeoMeta AND the assign); and early-return the assign tail for non-frontend templates — the display hook fires on BACKEND pages too. Also: PKPTemplateManager::addJavaScript has NO defer arg — passing one is silently ignored ({load_script} sits at end-of-body anyway). (5) **a11y findings of note**: `aria-labelledby` REPLACES a link's own text — passing a shared heading id to every full-issue galley names them all "Full Issue" AND erases the restricted-access screen-reader text (drop it or include a self-id); core skipLinks.tpl emits #homepageIssue/#homepageAnnouncements whenever the DATA exists, so a theme whose toggles hide those sections must keep the anchor ids alive or the skip links go dead; a copy-button's textContent swap is not announced unless the button is its own `aria-live=polite` region; add `summary` to the global :focus-visible selector list (it is the one interactive element everyone forgets).
- **A sidebar "Usage statistics" widget (total + this-month downloads & views) from the OJS 3.5 stats service.** The stats service (`app()->get('publicationStats')`) is the source: `getFilesTotals($args)` = file DOWNLOADS, `getTotals($args)` = submission VIEWS, each returning per-submission rows you `array_sum` on `->metric`. For a TIME WINDOW pass `dateStart`/`dateEnd` as `Y-m-d` strings — `['contextIds'=>[$id], 'dateStart'=>date('Y-m-01')]` gives this-calendar-month (end defaults to yesterday). Four heavy aggregates, so: (a) memoise per request (a class `?array $usageStatsMemo`) AND cache 1h in a plugin setting `updateSetting($ctxId,'usageStatsCache',[...],'object')`; (b) when you ADD fields to an existing cache shape, require a NEW key in the freshness check (`isset($cache['t'],$cache['mdownloads'])`) so a cache written by the prior version is treated as a miss and recomputed — otherwise the new figures read as 0 for up to the TTL after upgrade; (c) on failure write a back-dated NEGATIVE cache (incl. the new keys + an `ok=>false` flag) so a broken/slow stats backend retries in ~10 min, never per render; (d) return an `available` flag distinct from zero, and DROP the whole widget when totals are 0 (a live journal showing "0 downloads" is worse than no widget). Render hygiene: thousands-format the numbers in PHP with `number_format()` (Smarty has no guaranteed `number_format` modifier — do NOT rely on `{$n|number_format}`), pass formatted strings to the template; gate the heavy compute on the toggle AND skip the templates with no sidebar (the auth pages set isFullWidth). Reuse one `{include}` partial for BOTH the shared journalSidebar and the article-page aside (the article sidebar does not include journalSidebar.tpl, so a partial avoids duplicating the markup). Default the toggle OFF (the widget needs the usage-stats backend populated).
- **"I cleared the stat fields but the statistics block still shows" = an auto-fill branch fighting the admin.** A tempting "nice out-of-the-box" pattern is: build the homepage stats from the admin's fields, then `if (empty($stats)) { $stats = liveMetrics(); }`. It backfires badly because the stat NUMBER fields already DEFAULT to live tokens ({volumes}/{articles}/…), so a fresh journal is never empty — the only time `empty($stats)` is true is when the admin DELIBERATELY cleared every field, and that's exactly when the auto-fill repopulates it. The user reads the reappearing numbers as "hardcoded, won't go away." Fix: delete the auto-fill — the token DEFAULTS already deliver the out-of-the-box experience, and an emptied field set must yield an empty (hidden) block. Verified the cascade so the fix is sound: PKP `ThemePlugin::getOption()` returns a stored value via `isset($optionValues[$name])`, and `isset('')` is TRUE, so a field the admin cleared to '' is returned as '' (NOT the default) — the default is only used when the setting was never stored (`null`). So "clear field → '' → card dropped" works on its own; nothing else is needed. Related hardening in the same block: require the NUMBER (not "number OR label") when building each card, so clearing a number while a default label lingers doesn't leave a ghost card with a blank figure; and drop any card whose number is a TOKEN that resolved to <= 0 (a new journal must not show "0 Articles"). General principle: never let a convenience default re-assert itself over an explicit empty admin choice — empty is a decision, not a missing value.
- **Make a "static admin list" widget dynamic: a live trending-keyword cloud from real article metadata.** A sidebar keyword cloud typed by hand (with placeholder defaults like "machine learning") is the classic not-actually-dynamic widget. Replace it with an auto source: scan the last ~60 published submissions (bounded collector with `->limit(60)->orderBy('datePublished','DESC')`), pull each current publication's `getLocalizedData('keywords')` (3.5 controlled-vocab items are {name} objects/assoc-arrays — handle object/array/string), tally case-insensitively (`mb_strtolower` key, preserve first-seen display form), `arsort`, take the top N. Two integration musts: (1) **cache PER LOCALE** — keywords are localized, so an Arabic page must aggregate Arabic keywords; store a per-locale map in ONE plugin setting (`['ar'=>['t'=>ts,'kw'=>[...]], 'en'=>[...]]`) with a multi-hour TTL (keywords change slowly) so the heavy scan runs a few times a day, not per render; (2) **gate the compute** on the page actually rendering that sidebar (reuse the rendersSharedSidebar flag) so article/auth pages don't pay for it. Offer a `keywordsSource` radio (auto/manual) defaulting to AUTO — the manual textarea stays as the fallback, and its description should say it's used only in manual mode. Net effect: the cloud reflects what the journal actually publishes, self-maintains, and links each term to a search. General principle for "make everything dynamic": hunt option DEFAULTS that are sample content (keyword lists, index services, info links) — those are the tells that a widget is static when it could be derived from live data.
- **"Most read" sidebar widget: rank articles by views from the same stats service, resolving titles per-locale.** The stats service `getTotals(['contextIds'=>[$id]])` returns one row per submission with `submission_id` + `metric` (total views) — build a `[id=>metric]` map, `arsort`, then walk the top entries loading each via `Repo::submission()->get($id)` (skip non-published / wrong-context), pulling `getCurrentPublication()->getLocalizedTitle(null,'html')`. Cache the RESOLVED list (id+title+formatted views) PER-LOCALE in a plugin setting (6h TTL) because titles are localized; cap the candidate scan (e.g. 40) so a journal with many zero-view rows can't loop forever. Two reuse notes: this is the third widget on the same per-locale-cached-plugin-setting pattern (popular keywords, usage stats, most-read) — they share the structure (`['ar'=>['t'=>,…],'en'=>[…]]`, isset-the-new-key freshness guard, gate compute on the page rendering a sidebar) so it's worth a mental template; and ship each as a small `{include}` partial reused by BOTH the shared journalSidebar and the article aside, since the article page doesn't include journalSidebar.tpl. Default these stats-backed widgets OFF (they need the usage-stats backend populated) and self-hide when empty.
- **Per-article view/download counts inline on the article page — light, filtered stats + a self-pruning cache.** Unlike the journal-wide aggregates (which GROUP every submission and are heavy), a single article's metrics are a TARGETED lookup: `getTotals(['contextIds'=>[$id],'submissionIds'=>[$artId]])` for views and `getFilesTotals([... same ...])` for downloads (the stats service accepts a `submissionIds` filter). Display them as two more `.mb-item`s in the article meta bar (reuse the existing key/value metadata pattern; style the figures brass so they read as metrics not metadata), gated on a toggle (default OFF — needs the usage backend) and self-hidden when both are 0. Caching nuance distinct from the per-LOCALE widget caches: article metrics are locale-agnostic but PER-ARTICLE, so a growing map is the risk — store `['<artId>'=>['t'=>,'v'=>,'d'=>]]` in one plugin setting with a SHORT TTL (30 min) and PRUNE stale entries on every write (`foreach($cache as $k=>$v) if(expired) unset`), which bounds the map to roughly the articles viewed within the window. Net: the full stats picture is now journal-wide (homepage tokens + sidebar totals/monthly), ranked (most-read), and article-level (this paper's own numbers) — all from one service, each cached at the granularity that fits (1h journal totals, 6h per-locale rankings, 30min pruned per-article).
